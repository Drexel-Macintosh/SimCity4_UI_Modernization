{
 "summary": "The region bubble's Mayor Rating bar IS a real child window \u2014 the live dump simply never reached it (a depth-limit blind spot, not an absence). It is window id 0x4A553000, clsid 0xAA5D16A9 = cSC4WinAuraBar, design area (11,92)-(113,103) = 102x11, nested at depth 3 inside I-ca539340.ui (bubble root -> outer GZWinBMP -> inner GZWinBMP -> AuraBar). The painter is cSC4WinAuraBar::Draw at VA 0x00797CC0 (slot 88 of vtable 0x00AB64B8) \u2014 a DISTINCT routine from the HUD, which uses a plain GZWinBMP (GZWinBMP::Draw 0x009BC325) plus the controller 0x007E86C0-0x007E8A80. Zero shared code, disjoint art ids. The bug: AuraBar::Draw builds its SOURCE rect width from the WINDOW width and its row index from the IMAGE height \u2014 src = {L=(imgW-winW)/2, T=round(value*(imgH-1)), R=winW+L, B=T+1}, blitted into the full window rect. At f=2 the window is 204 wide but the code-bound art {0x856DDBAC,0x46A006B0,0x14416327} is still the stock 102x26, so the code requests a 204-px-wide source span from a 102-px-wide bitmap = exactly 2 periods = \"two runs side by side\". Number of copies == winW/imgW == the scale factor, exactly the task #49 / #55 LEFT1X law. Art 0x14416327 is code-bound (pushed at 0x007B517E), so the reference-driven selective-art build never saw it: it appears in 0 of 432 refmap.csv lines. Correctly-sized versions already exist unpackaged in tools\\upscale\\preview* (204x52 / 153x39 / 306x78). The fix is pure DATA and cannot touch the HUD bar.",
 "findings": [
  {
   "claim": "The live dump's \"NO child is the rating bar\" is a BLIND INSTRUMENT, not a negative result. The bar is a normal child window: id 0x4A553000, clsid 0xAA5D16A9, 1x area (11,92)-(113,103) = 102x11.",
   "evidence": "MEASURED. tools\\uiscripts\\extracted\\T-00000000_G-96a006b0_I-ca539340.ui contains `<LEGACY clsid=0xaa5d16a9 iid=IGZWinCustom id=0x4a553000 area=(11,92,113,103)>` nested inside the inner GZWinBMP area=(12,10,247,152), i.e. DESIGN DEPTH 3 below the bubble root 0x0a551c50. POSITIVE CONTROL for the null: the UiSpike RGKID dumper emits dotted paths; across the whole 759,126-byte <HOME>\\OneDrive\\Documents\\SimCity 4\\Plugins\\SC4UIScale.log there is exactly ONE 4-component row (`RGKID 11.0.4.0 id=0x00000000 vt=00ADF6A0 (26,22 382x114)`) and ZERO 5-component rows, so the dumper stops one level ABOVE the AuraBar. Confirming the same blind spot: the AuraBar's seven same-depth siblings 0x4A552000..0x4A552006 appear 0 times each in the log, while its shallower siblings (0x4A560000) appear. The brief's 10:45:28 dump likewise ends at the inner BMP (24,20 470x284).",
   "status": "MEASURED"
  },
  {
   "claim": "The region bar's painter is cSC4WinAuraBar::Draw at VA 0x00797CC0, reached only through slot 88 (+0x160) of the class's primary vtable 0x00AB64B8. It is the class's ONLY window-virtual override.",
   "evidence": "MEASURED. clsid->name registry .data VA 0x00B08FA0 holds {0xAA5D16A9, ptr->\"cSC4WinAuraBar\"}. Class registration at 0x004662E9: `68 20 7F 79 00  push 0x797F20` (factory); `E8 BD 18 33 00  call 0x797BB0` (GetClassID: bytes `B8 A9 16 5D AA C3` = mov eax,0AA5D16A9h; ret); `50 8B CE E8 38 7E 4A 00  push eax; mov ecx,esi; call 0x90E133`. Factory 0x00797F20 allocates 0xF8 bytes, calls ctor 0x00797E60, returns obj+0xE0. Ctor writes [this+0]=0x00AB64B8, [this+0xD8]=0x00AB64A0, [this+0xE0]=0x00AB6488, [this+0xE8]=double from 0x00A92D28 (=0.5), [this+0xF0]=0. Slot-by-slot diff of vtable 0x00AB64B8 against GZWinBMP 0x00ADF6A0 and GZWinBtn 0x00ADDAF0 over slots 0..119: identical everywhere except slots 0-5 (QI/AddRef/Release/etc), 55, 62, and slot 88 (+0x160) where AuraBar=0x00797CC0, GZWinBMP=0x009BC325, GZWinBtn=0x009B167D.",
   "status": "MEASURED"
  },
  {
   "claim": "THE DEFECT MECHANISM: AuraBar::Draw takes the source-rect WIDTH from the WINDOW and the source ROW from the IMAGE, then blits into the full window rect. Source span = winW regardless of the bitmap's actual width.",
   "evidence": "MEASURED, disassembly of 0x00797CC0 (eax = image rect ptr from `call [img+0x30]` at 0x00797CF3; ebx = &windowRect at this+0x24; [this+0xE8] = value; [this+0x68] = draw ctx). 0x00797D16-0x00797D28: `8B 78 08 / 8B 4B 08 / 8B 10 / 2B F9 / 8B 0B / 2B FA / 03 F9 / D1 FF` => edi = ((imgR-imgL) - (winR-winL)) >> 1 = (imgW - winW)/2, stored as src.L. 0x00797D2C-0x00797D47: `8B 50 0C / 2B 50 04 / 4A` => (imgH-1); `DB 44 24 0C  fild` ; `DC 8E E8 00 00 00  fmul qword [esi+0xE8]` ; `DC 05 28 2D A9 00  fadd qword [0xA92D28]` (=0.5) ; `E8 B8 71 25 00  call 0x9EEF04` (ftol) => src.T = (int)(value*(imgH-1)+0.5). 0x00797D4C-0x00797D70: `8B 13 / 8B 4B 08 / 2B CA / 03 CF` => src.R = winW + src.L = (imgW+winW)/2. 0x00797D5F/0x00797D65: `40` inc eax => src.B = src.T+1. 0x00797D60 `53 push ebx` passes &windowRect as the DESTINATION. 0x00797D79 `E8 42 0E 14 00  call 0x8D8BC0` (ctx, image, &src, &dst, 0,0,0). Note src.R-src.L == winW exactly, and src height == 1 (a 1-px LUT row stretched to winH). The generic blitter 0x008D8BC0 takes the general path at 0x008D8CA8 for our case (srcW=204 != 1, so the 1x1 fast path at 0x008D8C13 is skipped); its fallback contains explicit modulo/wrap normalisation of out-of-range coordinates (`99 F7 FF idiv edi` at 0x008D8D42 by srcW, `99 F7 FB idiv ebx` at 0x008D8D4B by srcH, negative fixup `8D 04 0E / 99 / F7 FF / 0F AF C7 / 2B F0` at 0x008D8D6D-0x008D8D82) \u2014 it tiles rather than clamps.",
   "status": "MEASURED"
  },
  {
   "claim": "The art is code-bound and still 1x: {type 0x856DDBAC, group 0x46A006B0, instance 0x14416327} ships as 102 x 26 and has NEVER been packaged at any scale factor.",
   "evidence": "MEASURED. Bind site inside the bubble populate routine sub_7B4B80: 0x007B517E `68 27 63 41 14  push 0x14416327`; 0x007B5183 `68 B0 06 A0 46  push 0x46A006B0`; 0x007B5190 `call 0x602B70` (builds an image-request object, stores group at +0xC, instance at +0x10); 0x007B51A7 `FF 52 0C  call [iface+0x0C]` = AuraBar::SetImage. Immediate 0x14416327 occurs exactly ONCE in the whole .text. Stock bitmap tools\\dbpf\\extracted\\SimCity_1\\T-856ddbac_G-46a006b0_I-14416327.png reads 102 x 26 from its IHDR. `grep -c 14416327 tools\\selective-safe\\refmap.csv` = 0 out of 432 lines (contrast: 0x14416322 line 283 and 0x14416326 line 287 are present, both UNSCALED/untouched, because those ARE .UI-referenced). It also appears in no package-list*.txt and nowhere in the DLL/PowerShell sources. Correctly sized upscales already exist but were never shipped: tools\\upscale\\preview = 204x52, preview-15x = 153x39, preview-3x = 306x78 (= round(102*f) x round(26*f) for f = 2 / 1.5 / 3).",
   "status": "MEASURED"
  },
  {
   "claim": "THE ARITHMETIC OF THE DOUBLING at f=2: src.L = (102-204)/2 = -51, src.R = 204-51 = 153, so a 204-px source span is requested from a 102-px-wide bitmap = exactly 2 periods = two runs side by side. Copy count == winW / imgW == the scale factor.",
   "evidence": "MEASURED inputs + arithmetic. imgW=102 (IHDR above). winW at f=2 = 204: the AuraBar's parent chain is measured doubled in the live log \u2014 bubble root 0x0A551C50 design 258x250 -> live 516x500, inner BMP design (12,10)-(247,152)=235x142 -> live (24,20 470x284); and the log's other bubble variant (unincorporated, script I-0a8cd184, root design (146,71)-(362,236)=216x165) reads live 432x330 = exactly 2x. Since AuraBar::Draw's source span is literally winW, the observed count of 2 repeats is itself the measurement that winW = 2*imgW. This is the identical trap signature already recorded in _tests\\REGRESSION.md lines 1321-1334 (\"DUPLICATED MENU ICON\", task #49): \"The count of visible copies tells you the scale ratio: 2 copies = 1x art in a 2x cell. It is ALWAYS a missing 2x override at the TGI actually being drawn.\"",
   "status": "MEASURED"
  },
  {
   "claim": "LOUD ANSWER \u2014 the routine is NOT shared with the HUD. The HUD Mayor Rating bar and the region bubble bar have zero code and zero art in common, so any fix aimed at 0x14416327 or 0x00797CC0 is physically incapable of regressing the (correct) HUD bar.",
   "evidence": "MEASURED, four independent separations. (1) CLASS: clsid 0xAA5D16A9 appears in exactly 1 of the 330 extracted .ui files (the region bubble I-ca539340); the HUD script I-2bc90671 contains only GZWinBtn x17, GZWinBMP x11, GZWinText x4, GZWinCustom x3, 0xc7a0e17e x3, GZWinGen, 0x89e1567c \u2014 no AuraBar. (2) PAINTER: HUD groove 0x8a517556 is a plain GZWinBMP -> draw slot 88 = 0x009BC325; region bar -> draw slot 88 = 0x00797CC0. (3) ART: HUD uses .UI-bound image={46a006b0,14015549} imagerect=(0,0,102,11) (refmap.csv line 97, EXCLUSIVE, 2x-in-place, referrers only I-2bc90671 / I-898897de); region uses code-bound 0x14416327. Immediate 0x14015549 occurs in .text only at 0x007E851D and 0x007ED224 (HUD/polls controllers); 0x14416327 only at 0x007B517F. (4) CONTROLLER: the HUD's 0x007E86C0-0x007E8A80 imul-by-7 arrow logic never touches the AuraBar \u2014 consistent with the user's A/B that RatingArrowPatch=0 leaves the region doubling unchanged.",
   "status": "MEASURED"
  },
  {
   "claim": "FORWARD CONTROL FLOW from the region side (the independent route requested): region-screen class cSC4WinRegionScreen (clsid 0xEA659793, vtable 0x00AB9260) -> slot 134 sub_7ACAD0 picks the bubble script and opens it -> sub_7B5E20 -> sub_7B59B0 -> sub_7B4B80 populate -> AuraBar SetValue + SetImage.",
   "evidence": "MEASURED. Vtable 0x00AB9260 identified by its Init at slot 4 = sub_7B1900, which at 0x007B192E does `68 93 97 65 EA  push 0xEA659793` then `FF 90 00 01 00 00  call [eax+0x100]`; ctor sites writing 0x00AB9260 at 0x007B0C20 and 0x007B1205. Slot 134 (+0x218) = sub_7ACAD0, which at 0x007ACC34-0x007ACC50 selects the bubble variant: `68 50 1C 55 0A  push 0x0A551C50` + `68 40 93 53 CA  push 0xCA539340` (established city, HAS the AuraBar) vs the else-branch `push 0x0A551C50` + `68 84 D1 8C 0A  push 0x0A8CD184` (unincorporated, verified to contain NO 0xaa5d16a9 and no 0x4a553000), then `E8 CB 91 00 00  call 0x7B5E20`. Caller graph from tools\\uimap\\_work\\edges.json: sub_7B5E20 -> sub_7B59B0 (site 0x007B5E55) -> sub_7B4B80 (site 0x007B5B9A, sole caller). Inside sub_7B4B80: 0x007B4FB5 `FF 50 38 call [eax+0x38]` returns the integer mayor rating; 0x007B4FCE-0x007B4FDE `DB 44 24 18 fild / DC 0D B8 98 AB 00 fmul qword [0xAB98B8] (=0.005 = 1/200) / DC 05 28 2D A9 00 fadd qword [0xA92D28] (=0.5) / DD 5C 24 18 fstp` => value = rating/200 + 0.5, i.e. rating -100..+100 mapped to 0.0..1.0. That double is then fld'd at 0x007B516C and passed to AuraBar::SetValue.",
   "status": "MEASURED"
  },
  {
   "claim": "HOOK / IDENTIFICATION HANDLES if a code route is ever wanted (it should not be needed).",
   "evidence": "MEASURED. Object: size 0xF8; [this+0x24]={L,T,R,B} window rect; [this+0x68] draw context; [this+0xE0] custom-interface vtable ptr = 0x00AB6488; [this+0xE8] double value; [this+0xF0] image. Custom iface vtable 0x00AB6488: slot 3 (+0x0C) = 0x00797E10 SetImage, slot 4 (+0x10) = 0x00797C20 SetValue(double). Custom IID = 0x4A5D1208 (occurs exactly once in the exe, at the push at 0x007B514D; the fetch is `FF 92 94 00 00 00 call [vt+0x94]` = GetChildAsRecursive(0x4A553000, 0x4A5D1208, &out) at 0x007B515E). Primary vtable slot to repoint: .rdata VA 0x00AB6618 (= 0x00AB64B8 + 0x160), currently 0x00797CC0. Prologue bytes at 0x00797CC0 for a detour: `83 EC 14 56 8B F1 8B 86 F0 00 00 00`. Instance can be positively identified by [this+0]==0x00AB64B8, or by window id 0x4A553000. A pure byte patch is NOT clean here: forcing src.L=0 by turning `D1 FF` (sar edi,1) at 0x00797D26 into `33 FF` (xor edi,edi) still leaves src.R = winW, and by 0x00797D4C the image-rect pointer in eax has been clobbered by the ftol call at 0x00797D47 \u2014 so a code fix needs a full detour, not an in-place immediate edit.",
   "status": "MEASURED"
  },
  {
   "claim": "RECOMMENDED FIX (pure data, blast radius = one bitmap): add a 2x-in-place override for {0x856DDBAC, 0x46A006B0, 0x14416327} (204x52) to the selective-art package, and the matching 153x39 / 306x78 for the 1.5x / 3x tiers. Then imgW == winW at every tier, src.L becomes 0, src.R becomes winW, and the LUT row index round(value*(imgH-1)) still lands on the same visual state.",
   "evidence": "HYPOTHESIS (mechanism is measured; the outcome is a prediction awaiting an in-game A/B). It follows directly from the measured arithmetic: with imgW = round(102*f) = winW, src.L = 0 and src.R = winW, so exactly one period is sampled. Doubling the height only raises the LUT resolution (26 -> 52 states) and is harmless because the source is always 1 row stretched to winH. Precedent: the same class of code-bound-art miss is already documented for cSC4WinTrendBar's 0x14015580 / 0x14015584 and the mayor faces 0x14315e60 / 0x14315e62 in tools\\research\\DYNAMIC-CONTROLS.md Q4. Two things to watch: (a) the assets exist only in tools\\upscale\\preview* and must be added to the packaging list explicitly, since refmap.csv is reference-driven from .UI scripts and will never discover a code-bound TGI; (b) a duplicate instance 0x14416327 also exists under group 0x1ABE787D (also 102x26) \u2014 scope the override to group 0x46A006B0, which is the only group the code pushes.",
   "status": "HYPOTHESIS"
  }
 ],
 "verdictOnHypothesis": "SUPPORTS"
}