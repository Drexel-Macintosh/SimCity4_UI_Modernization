[
 {
  "key": "v2:ad2ba76b9718e56647e9d22944f502675c400be3243aced837ac0b00da80385d",
  "result": {
   "lane": "Lane: does the runtime sweep actually SCALE window 0x8A517556, and does that differ between MAYOR MODE and GOD MODE? — VERDICT: the sweep DOES scale it, at every tier, and there is NO mode gate anywhere on its subtree. The stated lead (\"window stays 102 wide while art/crop are 153, 102/153 = 0.667 = only half filled\") is REFUTED by live measurement at both 1.5x and 2x.",
   "findings": [
    {
     "claim": "THE LEAD IS REFUTED. The runtime sweep scales 0x8A517556 to exactly the pre-scaled crop at 1.5x — window 153x17 against imagerect (0,0,153,17). The window is never 102 wide at 1.5x.",
     "evidence": "LIVE DRAWPROBE (slot 88 = the paint entry), positive control 'DRAWPROBE live' present in all three runs: 1.5x `_tests/captures/SC4UIScale-2026-08-16-153245.log:496` `win=0x8A517556 rect=(180,85 153x17) class=00ADF6A0 [paint 513]`; same rect again in `SC4UIScale-2026-08-16-174827.log:497` and `:1282`. 2x `_tests/captures/SC4UIScale-2026-08-16-172018.log:551,631` `rect=(240,114 204x22)`. Design is (120,57) 102x11 in the LIVE declaring script `tools/uiscripts/extracted/T-00000000_G-96a006b0_I-2bc90671.ui:22` (live-script identity independently pinned at `src/UiSpike.cpp:5738-5747`). 120*1.5=180, 102*1.5=153, RoundHalfUp(11*1.5)=17; 120*2=240, 102*2=204, 11*2=22. Sizing path: `src/UiSpike.cpp:17370-17395` — the #148 leaf rule fires because GetChildCount()==0, so size = ScaleRound(w,f) x ScaleRound(h,f), NOT edge-derived. Shipped crop, read off disk: `tools/selective-safe/stage-15x/...I-0x2bc90671.ui` imagerect=(0,0,153,17); stage (2x) (0,0,204,22); stage-3x (0,0,306,33). Shipped sheet {46a006b0,14015549}, PNG IHDR read at the package-list offsets: 1x-source 102x26 -> 1.5x 153x39 (`tools/packages/15x/z_SC4UIScale_SelectiveArt-15x.dat` @0x00373C51, `tools/selective-safe/package-list-15x.txt:160`), 2x 204x52, 3x 306x78 — exact NN, no CellUnit snap on either axis.",
     "tierSignature": "window: 1x 102x11 | 1.5x 153x17 (MEASURED) | 2x 204x22 (MEASURED) | 3x 306x33 (derived). crop: 102x11 | 153x17 | 204x22 | 306x33. sheet: 102x26 | 153x39 | 204x52 | 306x78. Window == crop at every tier.",
     "confidence": "measured"
    },
    {
     "claim": "THERE IS NO MODE GATE. The subtree root 0xE9889775 sits in exactly ONE list — kAlwaysScaleCityIds — and in no list that would stop, truncate or mode-gate the recursion. It is scaled unconditionally on the load-time 'city' pass, WHILE HIDDEN.",
     "evidence": "Membership check over every id list in `src/UiSpike.cpp`: kAlwaysScaleCityIds (:5271) MEMBER at :5334 'composite status panel'; kCityHudFamilyIds (:5755) MEMBER as the LEADER. NOT a member of kNeverScaleIds (:4815), kDataScaledSubtreeIds (:5410 — this is the one that would stop recursion, per law 96 / #170), kCityDialogIds (:14751), kGodToolFlyoutIds (:4936), kSubFlyoutIds (:5248), kFontSizedIds (:5542), kAdviceListNeverTouchIds (:5639), kGodPanelIds (:4972), kRegionPanelIds (:4781), kOwnsBackgroundSheet (:14462). IsMayorOnlyFlyoutId (:5218) resolves through FindMayorDock, which has no entry for 0xE9889775. 0x8A517556 itself is in NO list at all. The sweep's only visibility gate is `src/UiSpike.cpp:10306-10316`, and kAlwaysScaleCityId is an explicit exception to it. MEASURED consequence: in `_tests/captures/SC4UIScale-2026-08-16-174827.log:434-435` the sweep fires at 17:47:01.741 while the HUD is still hidden (the VWKID block at 17:47:01.859 does NOT list 0xE9889775), and the bar is already 153x17 when mayor mode first paints it 3 seconds later at :497.",
     "tierSignature": "panel 0xE9889775 (139,1413 880x180) -> 1.5x (209,1319 1320x270) | 2x (278,1226 1760x360) | 3x (139,1973 880x180)->(417,1599 2640x540) — exact at all three.",
     "confidence": "measured"
    },
    {
     "claim": "COVERAGE IS COMPLETE AND IDENTICAL AT EVERY TIER: 209 recorded sweep passes across the whole capture corpus, every single one reporting '30 windows scaled', and the live tree under 0xE9889775 contains exactly 30 nodes — 0x8A517556 among them. Not one pass ever scaled fewer.",
     "evidence": "`grep -rh 'panel 0xE9889775 - ' _tests/captures/` yields exactly one distinct line, 209 times: 'UiSpike: city panel 0xE9889775 - 30 windows scaled.' n is the mutated-window counter (`src/UiSpike.cpp:10357-10360`, count++ paired with every SetW/SetH/GZWinMoveTo). Node count taken from the live pre-sweep tree dump `_tests/captures/SC4UIScale-2026-08-15-180919.log:1195-1224` — programmatically walked by indentation: 30 nodes in the subtree (root incl.), and `0x8A517556 pos(120,57) size(102x11) children=0` is one of them, under anonymous parent `id=0x00000000 pos(2,0) size(878x182)`. 30 mutated == 30 present, so nothing in that subtree is skipped.",
     "tierSignature": "'30 windows scaled' at 1.5x, 2x and 3x alike — the count does not change with the factor.",
     "confidence": "measured"
    },
    {
     "claim": "'MAYOR MODE' IS NOT A DISCRIMINATOR — it is the only mode in which this widget exists on screen. 0xE9889775 IS the project's own mayor-mode predicate, so there is no god-mode instance of the bar to differ from.",
     "evidence": "`src/UiSpike.cpp:13009-13016`: 'USE INSTEAD: the composite mayor HUD 0xE9889775 ... shown in mayor mode and hidden in god mode' -> `const bool mayorModeActive = (mayorHud != nullptr && mayorHud->IsVisible());`. LIVE CONFIRMATION with a working positive control: in `_tests/captures/SC4UIScale-2026-08-16-174827.log`, the bar's last paint is 17:48:13.559 (:1282); the VWKID block at 17:48:13.605 (:1291) lists 0xE9889775 visible; by 17:48:21.687 (:1552) DGPKID reports `E9889775 ... vis=0` and the bar never paints again — while the god-mode sun button 0x2988BC85 keeps producing DRAWPROBE lines through 17:48:25.287, proving the probe budget was NOT exhausted and the hook was still live. Parent chain is mode-independent and identical in all four binding scripts: 0xE9889775 (GZWinGen root) -> anonymous GZWinBMP with NO id -> 0x8A517556. Files/lines: `T-00000000_G-96a006b0_I-2bc90671.ui:2/:4/:22`, `..._I-898897de.ui:2/:4/:22`, `T-00000000_G-08000600_I-2bc90671.ui:2/:4/:6`, `..._I-898897de.ui:2/:4/:18`. DPROBE independently reports `par=0x00000000` (`_tests/captures/2026-08-03-101-15x-CRASH.log:291`).",
     "tierSignature": "Mode-independent: the bar is scaled while HIDDEN (god mode) and painted only while VISIBLE (mayor mode), at 1.5x and 2x alike.",
     "confidence": "measured"
    },
    {
     "claim": "THE STATED GAP IS CLOSED: every 2026-08-16 DRAWPROBE capture of 0x8A517556 — including the 1.5x one quoted at REGRESSION.md:10547 — was taken in MAYOR MODE.",
     "evidence": "VWKID is a VISIBLE-ONLY, change-only dump of direct 3D-view children (`src/UiSpike.cpp:15889-15918`: `if (!w || !w->IsVisible()) { continue; }`), so 0xE9889775 appearing in a VWKID block is proof the mayor HUD was on screen. Bracketing all five bar paints against the nearest VWKID block: 153245 15:32:16.166 -> next VWKID 15:32:16.212 HUD PRESENT (46 ms); 172018 17:19:52.148 -> 17:19:52.207 PRESENT (59 ms); 172018 17:19:54.527 -> preceding block 17:19:52.207 already PRESENT; 174827 17:47:04.718 -> 17:47:04.769 PRESENT (51 ms); 174827 17:48:13.559 -> 17:48:13.605 PRESENT (46 ms). Zero paints occur while the mayor set is absent. Each of those blocks also carries the full mayor trio (0x6A64E3C0 opinion polls + 0x69E40A1F mayor toolbar column), and the immediately preceding god-mode blocks carry 0xC98F49F1 instead with no 0xE9889775 (e.g. `SC4UIScale-2026-08-16-174827.log:1276-1280` at 17:48:13.247).",
     "tierSignature": "1.5x paints (153245, 174827 x2) and 2x paints (172018 x2) are ALL mayor mode. No god-mode paint of this id exists in the corpus.",
     "confidence": "measured"
    }
   ],
   "positiveControl": "Three independent positive controls, all present, so none of the nulls below are silent failures. (1) PROBE CHANNEL: every capture carrying DRAWPROBE data opens with the 'UiSpike: DRAWPROBE live - slot 88 paint #1 observed' line (e.g. SC4UIScale-2026-08-16-153245.log:470, 174827:471), so the hook is installed AND executing before any watched id is judged. (2) BUDGET NOT EXHAUSTED: in 174827 the god-mode sun button 0x2988BC85 is still emitting DRAWPROBE lines at 17:48:25.287, 12 s after the mayor bar's last paint at 17:48:13.559 — so the bar's silence after the mode switch is the mayor HUD going hidden, not the probe running dry. In every DRAWPROBE capture the ids 0xC988BC79 (mayor's-hat button) and 0x4988BC6A (people button) report, so the watch list is live. (3) NULL SCOPE STATED HONESTLY: 0x8A517556 does NOT appear in the eight earlier 2026-08-16 DRAWPROBE captures (094034, 095357, 111256, 131006, 141324, 142828, 143606, 152505) — that is NOT evidence of anything, because the id was only added to kDrawWatch for this defect (src/UiSpike.cpp:3483, comment dated 2026-08-16); those builds never watched it. The three runs that do watch it all report it.",
   "gaps": "1. NO LIVE 3x MEASUREMENT of 0x8A517556. The id entered kDrawWatch only on 2026-08-16 and every 3x capture predates that build, so the 3x window size 306x33 is DERIVED from ScaleRound(102,3)/ScaleRound(11,3) plus the shipped crop (0,0,306,33), never observed. The 3x sweep line 'panel 0xE9889775 (139,1973 880x180) -> (417,1599 2640x540) / 30 windows scaled' IS measured, so subtree coverage at 3x is measured even though the leaf rect is not.\n\n2. AN OLDER BUILD MEASURED 153x16, NOT 153x17. `_tests/captures/2026-08-03-101-15x-CRASH.log:291`, `2026-08-03-15x-DATAVIEW-CRASH-1600x1200.log:275` and `2026-08-03-TIER15X-dashboard-broken-SC4UIScale.log:291` all show `DPROBE ... id=0x8A517556 ... 153x16 vis=1`. Today it is 153x17, matching the crop. The one-pixel height change (#161/#166/#148 rounding work) is not investigated here and is far too small to be 'only half filled', but it means pre-2026-08-16 captures must not be quoted for this window's geometry.\n\n3. I DID NOT MEASURE THE BAR'S CONTENT, only its box — that is the other lane. One derived observation worth handing over, stated no stronger than the arithmetic: sub_7E8510 sizes its generated buffer from the SOURCE SHEET (GetWidth/GetHeight), not from the window. sheetH/windowH is 26/11 at 1x, 52/22 at 2x, 78/33 at 3x — all exactly 26/11 — but 39/17 at 1.5x, because 26*1.5=39 is exact while 11*1.5=16.5 rounds UP to 17. 1.5x is the ONLY tier where that ratio departs from the 1x contract. Width is unaffected (153 sheet == 153 window == 153 crop), so this cannot explain a horizontal half-fill on its own; it is a vertical-axis discrepancy of exactly the #177 family. Also worth noting for the art lane: {46a006b0,14015549} escapes BOTH #179 no-snap derivations — `oneone` keys on area==art (102x11 vs 102x26, no match) and the crop-vs-art rule keys on 1x imagerect==(0,0,artW,artH) ((0,0,102,11) vs 102x26, no match) — yet the shipped 1.5x sheet measures 153x39, i.e. it happened not to be snapped anyway. That is luck, not coverage.\n\n4. NOT CHECKED: whether cIGZWinBMP::SetBitmap (iface vt+0x10, 0x7E8700) resets or preserves the .UI imagerect crop after the controller installs its runtime buffer. If SetBitmap clears the crop, the shipped imagerect stops mattering at runtime and every crop-based argument in this defect's history is moot. That is a one-function disassembly read and it belongs to whoever owns the content lane.\n\n5. RATEANCHOR (CodePatches, sub_7E8510, `src/CodePatches.cpp:3973/4058`) is factor-correct at both tiers and is NOT a suspect: step=11 at 1.5x, step=14 at 2x, arrow seated at (98,58) 21x9 in both. It touches the decline ARROW (0xCA5A415E / 0x6A5A4156), never the groove."
  }
 },
 {
  "key": "v2:5b226d411b1e817eb5ff9cfbf340fccb41e6362af72b34a546e0d8d9e015423d",
  "result": {
   "lane": "Lane: script/group identity — which of the four .UI scripts carrying widget 0x8A517556 is live in mayor mode, and whether our pipeline treats it differently from the god-mode one.",
   "findings": [
    {
     "claim": "The city-HUD mode selector is a 5-case switch in the exe, and the MAYOR-mode HUD script is instance 0x2BC90671. God mode uses entirely DIFFERENT scripts (0x69E3D347 and 0xAA53E3EA). The four-script set is NOT mayor-vs-god.",
     "evidence": "SimCity 4.exe .text VA 0x7EE241: `cmp eax,4 / ja default / jmp [eax*4+0x7EF1D8]`, eax being a 0..4 loop counter (`xor eax,eax; mov [esp+0x10],eax` immediately before). Jump table at 0x7EF1D8 = {0x7EE230, 0x7EE248, 0x7EE254, 0x7EE260, 0x7EE23C}. Each case is `mov edi,<script instance>; mov ebp,<toolbar root id>`: idx0 = 0x69E3D347 + 0xC991EDA8 (god toolbar tile strip), idx1 = 0x2BC90671 + 0x69E40A1F (mayor toolbar column), idx2 = 0xAA1F1F57 + 0xCA1F1D9C (My Sims), idx3 = 0x49889894 + 0xC98F49F1 (city panel variant), idx4 = 0xAA53E3EA + 0x0A78827A (founded-city god toolbar). The toolbar-root names are this project's own measured labels (src/UiSpike.cpp:4973-4999, 5294, 5331-5345). 0x898897DE appears ZERO times in the exe (positive control: 0x2BC90671 appears once, root 0xE9889775 seven times, bar 0x8A517556 once) — it is a dead pre-Rush-Hour twin (5 dashboard buttons vs 6, hard-coded English captions vs the live '##########' runtime placeholders). Independently corroborated live at 1.5x: SC4UIScale-2026-08-16-174827.log lines 468/489 report RCI columns 0x09D27EB0/0x29D27EC0/0x49D27ED0 at x=394/409/424, which is 1.5 x the 2bc90671 design x of 263/273/283; 898897de's 278/288/298 would predict 417/432/447 and is refuted.",
     "tierSignature": "Mode selection is tier-independent — the same switch runs at 1x/1.5x/2x/3x; nothing in it reads the scale factor.",
     "confidence": "measured"
    },
    {
     "claim": "Group 0x08000600 is not a mode and not a live variant at all — it is a vestigial 800-wide layout set, a strict 10-instance DUPLICATE subset of group 0x96A006B0, never named by the exe. The live group is 0x96A006B0 in every mode.",
     "evidence": "DBPF index census over all 9 install archives: group 0x08000600 has exactly 10 unique instances game-wide (all in SimCity_1.dat), group 0x96A006B0 has 271; the set difference 08000600 - 96A006B0 is EMPTY. Root geometry confirms the layout split: 08000600 roots span x=140..798 (800-wide screen) and 96A006B0 roots span x=140..1020 (1024-wide). Byte scan of SimCity 4.exe finds the dword 0x08000600 exactly once, UNALIGNED at file 0x71E1FF inside a .data run of 16-bit values (`00 3a 00 08 00 ba 00 08 ...`) — a coincidence, not a constant. Positive control: 0x96A006B0 occurs 75 times, 22 of them 4-aligned.",
     "tierSignature": "Both groups are edited identically by the builder at 1.5x/2x/3x, so the dead group costs 4 wasted entries in each shipped dat but cannot change what renders.",
     "confidence": "derived"
    },
    {
     "claim": "There is NO god-mode instance of the Mayor Rating bar. Widget 0x8A517556 is declared only by the mayor script 0x2BC90671 (and its dead twin 0x898897DE) — so the live DRAWPROBE that measured 153x17 was necessarily the mayor-mode window. The stated gap is closed.",
     "evidence": "grep for '8a517556' across all 281 extracted .UI scripts returns exactly 4 files, all instance 2bc90671/898897de (positive control: the scan DID find 4 hits, and all five mode scripts are present in the extracted set). Root census of the five live mode scripts: 0x69E3D347 declares only root 0xC991EDA8; 0xAA1F1F57 declares the My Sims roots; 0x49889894 declares 0xC98F49F1; 0xAA53E3EA declares 0x0A78827A — none declares 0xE9889775 or contains 0x8A517556. Only 0x2BC90671 declares roots 0xE9889775 + 0x69E40A1F. The probe line SC4UIScale-2026-08-16-174827.log:497 `DRAWPROBE win=0x8A517556 rect=(180,85 153x17) class=00ADF6A0 [paint 557]` is a PAINT-time hook, so the window was visible on screen at that moment.",
     "tierSignature": "Same probe at 2x reads 204x22 (SC4UIScale-2026-08-16-172018.log:551) — window == crop at both tiers.",
     "confidence": "measured"
    },
    {
     "claim": "All FOUR scripts are present in the DEPLOYED z_SC4UIScale_SelectiveArt-15x.dat and their payloads are byte-identical to stage-15x. Nothing is left on stock 1x geometry through a staging miss.",
     "evidence": "Direct index read of <PROJECT-ROOT> 4\\Plugins\\z_SC4UIScale_SelectiveArt-15x.dat: 655 entries, 89 of type 0x00000000. All four TGIs HIT: {0,0x08000600,0x2BC90671} 28296 B, {0,0x08000600,0x898897DE} 25806 B, {0,0x96A006B0,0x2BC90671} 25770 B, {0,0x96A006B0,0x898897DE} 26878 B. SHA-256 of each stored payload equals the SHA-256 of the corresponding tools/selective-safe/stage-15x file (payloads are stored uncompressed, sizes match exactly). All four shipped payloads carry `id=0x8a517556 ... imagerect=(0,0,153,17)`. Positive control: three named non-target type-0 entries listed from the same index (0x09923283, 0x2A2AED99, 0xAA1F1F57), and the ItemIcons dat correctly reports 0 type-0 entries.",
     "tierSignature": "Same 4/4 HIT in the 2x and 3x siblings (z_SC4UIScale_SelectiveArt-2x/3x.dat.x1-disabled), sizes 28297/25813/25771/26878 and 28300/25816/25774/26881.",
     "confidence": "measured"
    },
    {
     "claim": "THE LEAD IS REFUTED. The builder treats all four scripts identically (imagerect x factor, font name->GUID, `area` NEVER touched), all four declare the SAME two roots, 0xE9889775 is runtime-swept and NOT in kDataScaledSubtreeIds, and the sweep demonstrably reached the bar at 1.5x: the window is 153 wide, not 102.",
     "evidence": "Attribute-level diff pristine-vs-staged for all 4 scripts at all 3 tiers: the ONLY changed attributes are `imagerect` (9-11 per file), `font` (3-5, name->GUID per FONTS-AND-DIALOGS), and one `image` retarget in the dead 08000600/898897de. Counts are byte-identical across 15x/2x/3x. `area` is unchanged on every node of every file. All four declare the same top-level roots 0xE9889775 (GZWinGen) and 0x69E40A1F (clsid 0x89E1567C). src/UiSpike.cpp:5334 lists 0xE9889775 in the pre-scale-while-hidden set and :5756 makes it the LEADER of kCityHudFamilyIds; it is absent from kDataScaledSubtreeIds (src/UiSpike.cpp:5410-5576), so the sweep RECURSES into it. Live at f=1.50 (log line 2: ScaleFactor=1.50, render 2400x1600): line 434 `panel 0xE9889775 (139,1413 880x180) -> (209,1319 1320x270)` = exactly x1.5, line 435 `30 windows scaled`, and line 497 DRAWPROBE 0x8A517556 = 153x17, which equals the shipped crop (0,0,153,17). 102/153 = 0.667 is therefore NOT happening.",
     "tierSignature": "crop and window agree at every tier: 1x 102x11, 1.5x 153x17 (11x1.5=16.5 rounds up), 2x 204x22, 3x 306x33.",
     "confidence": "measured"
    }
   ],
   "positiveControl": "Four separate controls, each stated before its null was accepted. (1) DBPF index reader: it reports 89 type-0x00000000 entries in the deployed 15x SelectiveArt dat and names three arbitrary non-target ones (0x09923283, 0x2A2AED99, 0xAA1F1F57) before I trusted any MISS; the DialogStatic dat's genuine MISS for all four is therefore a fact, and ItemIcons correctly reads 0 type-0 entries. (2) Exe dword scan: before accepting \"0x898897DE appears 0 times\", the same scanner found 0x2BC90671 once, root 0xE9889775 seven times, bar 0x8A517556 once, and 0x96A006B0 75 times (22 aligned) — it can see UI ids as immediates. (3) Script-content scan: before accepting \"no god-mode script contains 0x8A517556\", the same grep returned 4 positive hits, and all five live mode scripts were verified present in tools/uiscripts/extracted. (4) Live instrument: the DRAWPROBE at 1.5x is not a null at all — it printed 153x17 for the target and simultaneously printed 0xC988BC79 at 90x69, so the hook was proven executing on more than one window in the same second.",
   "gaps": "1. MY LANE'S HYPOTHESIS IS DEAD — do not spend more on it. The window is 153 wide with a 153-wide crop at 1.5x, measured at paint time; there is no unswept-root, wrong-group, unstaged-script or divergent-builder-rule story left. Any remaining explanation must live in the CONTROLLER or the ART, not in script selection.\n\n2. THE UNEXAMINED LEVER I FOUND, and it is aimed straight at sub_7E8510: we already ship a code patch ON that exact function. `CodePatches: RATEANCHOR installed on sub_7E8510 007E8510 (factor 1.50, mode 2: log + re-anchor)` — and its runtime behaviour DIFFERS between the two tiers. At 1.5x it logs ONCE (`cached=(98,58) arrow L/T=(98,58) 21x9 step=11 stale=0 write=1 arms=0`, 174827.log:222). At 2x it logs TWICE, the second with `arrow L/T=(196,116) 2x18 step=14 stale=1 write=1` (172018.log:630) — i.e. at 2x the patch DETECTS a stale anchor and re-anchors, and at 1.5x it never does. `step=11` vs `step=14` is also not a clean 1.5-vs-2.0 ratio of the same base. Whoever owns the controller lane should read RATEANCHOR in src/CodePatches.cpp before disassembling anything, because a live patch that behaves differently at the two tiers is the only asymmetry I found between a tier the user calls correct and a tier the user calls broken. Note it must not be assumed to be only the decline-arrow fix (#130) just because it logs arrow coordinates — it is installed on the BAR's generator.\n\n3. NOT ESTABLISHED, and I did not try: whether the composite HUD 0xE9889775 remains visible after switching to god mode inside a founded city. It is declared only by the mayor script, so it is created once; whether the game hides it on mode switch is unknown. This does not affect any finding above (the probe caught it painting), but it does mean \"the bar is a mayor-mode-only widget\" is proven at the DECLARATION level, not at the visibility level.\n\n4. FOUR WASTED ENTRIES, not a defect: the dead group 0x08000600 pair is edited and packed into every tier's dat. Harmless (the exe never names the group), but it is 4 of the 89 UI scripts we ship, and both dead files reference the same art TGIs as the live ones. I checked the one way that could have bitten us — a shared-art conflict leaving {46a006b0,14015549} at 1x — and it did not happen: the crop is scaled to (0,0,153,17) in all four shipped payloads."
  }
 },
 {
  "key": "v2:b8e6c8b47da9895331d5d6d46877a44aa3fb64c8eb72513b46b7bd1f73e5cb74",
  "result": {
   "reason": "REFUTED AS STATED — the arithmetic is sound but its scope is a fraction of what is claimed.\n\nWHAT I RE-MEASURED AND CONFIRMED (conceded):\n- 209 lines, exactly one distinct message text: \"UiSpike: city panel 0xE9889775 - 30 windows scaled.\" across 195 files (184 files x1, 8 x2, 3 x3 = 209). Correct.\n- My own indentation walk of _tests/captures/SC4UIScale-2026-08-15-180919.log:1195 gives exactly 30 nodes in the subtree (root incl.), with \"id=0x8A517556 pos(120,57) size(102x11) children=0 vis=1\" among them, under anonymous parent id=0x00000000 pos(2,0) size(878x182) children=8. Correct.\n- Counter semantics are as described: increments at src/UiSpike.cpp:14597 (root), :17253 (center-leaf, returns), :17403 (font-sized, returns), :17424 (normal else-branch) — mutually exclusive, at most ONE per window per visit. The frame-guard tombstone at :17410 deliberately does NOT count, so a skipped window really would drop the total. kMaxWindows=1500 (:4759), so 30 is not a saturated cap. Neither count-without-resize path can apply to this window (spikeCenterLeafMaxPx=48 < 102; 0x8A517556 absent from kFontSizedIds). Within one session whose tree is 30 nodes, 30==30 does mean nothing was skipped.\n\nWHY IT IS REFUTED ANYWAY — FOUR MEASURED DEFECTS:\n\n1. THE DENOMINATOR EXISTS AT ONE TIER ONLY. I scanned every file in _tests/captures/ for a tree dump of 0xE9889775: there are EXACTLY TWO in the whole corpus — SC4UIScale-2026-08-15-180919.log:1195 and SC4UIScale-2026-08-15-182552.log:1279. Both logs read \"ScaleFactor=1.50\", both same evening ten minutes apart, same city, same monitor 2400x1600, both with the root at pos(139,1413) size(880x180) vis=0. There is NO tree dump at 2x and NO tree dump at 3x anywhere. So at the two USER-CONFIRMED-GOOD tiers only the MUTATED half of the identity was ever recorded; the PRESENT half was never measured. A 31-node tree with 30 mutated prints a byte-identical line. The identity is closed at 1.5x and nowhere else.\n\n2. THE \"TIERS\" LINE IS A CONSTANT SOLD AS CORROBORATION. I verified the tier spread myself (61 files 2.00, 55 files 1.50, 53 files 2.00+3.00, 15 files 3.00, 9 files 1.50+2.00). The count is indeed 30 everywhere. But a number identical at the broken tier and at both working tiers has ZERO discriminating power on the question actually asked — it cannot separate a swept window from an unswept one, so it cannot close the lead. It is unfalsifiable, not confirmatory. (Note 30 is not even a distinctive value: sibling root 0x6A61E29F reports \"30 windows scaled\" in the very same pass.)\n\n3. \"NOT ONE PASS EVER SCALED FEWER\" IS A FILTERED SAMPLE. The log call is gated `if (n > 0)` (UiSpike.cpp:10362-10368) at LogLevel::Debug. A pass that scaled ZERO windows prints nothing and is structurally absent from the 209; 20 of the 215 log files carry no such line at all. So the corpus is \"209 passes that mutated at least one window\", not \"209 passes\". And 184 of 195 files carry exactly ONE line — the panel is swept once per city load and never reported again, so this is evidence about the LOAD-TIME sweep only, not about the panel's state at any later moment.\n\n4. MODE — THE VERY GAP THE BRIEF FLAGGED — IS UNTOUCHED, AND UNTOUCHABLE. I grepped both dump logs and all three DRAWPROBE logs for any mayor/god mode marker: ZERO hits in every one. No instrument in this corpus records mode at all, so there is no positive control for mode anywhere — a textbook NULL IS NOT EVIDENCE. The claim asserts coverage \"at every tier\" and lets that read as \"in every mode\"; its evidence cannot address mayor mode even in principle. Both dumps show the root vis=0, i.e. the pre-sweep boot state.\n\nADDITIONALLY: no log line anywhere in 215 files ever names 0x8A517556 as scaled — the ordinary ScaleSubtree mutation path emits no per-window line, so the assertion \"0x8A517556 was one of the 30\" is a pure arithmetic inference, never a direct observation. The only DIRECT measurements of the live window are 3x DPROBE \"153x16\" (2026-08-03 logs, 1.5x) and 5x DRAWPROBE (153x17 at 1.5x; 204x22 at 2x) — note 153x16 vs 153x17 is two different heights for the same window at the same tier (offset-parity on the odd height 11), while the WIDTH is 153 in every one. Width is parity-immune here because 102 is even: R(a+102)-R(a) = 153 for either parity of a.\n\nVALUE TO THE PARENT — the claim, correctly scoped, NARROWS the lead rather than closing it: at 1.5x the load sweep provably did reach every node of this subtree, and it runs once per launch and never reports again. So if the bar is genuinely short in mayor mode at 1.5x, the loss cannot be at the load sweep. Look instead at what happens AFTER it — cIGZWinBMP::SetBitmap from sub_7E8510 at 0x7E8700, which regenerates the source buffer at runtime on every rating change, on a widget whose draw is dst-follows-src. Nothing in this corpus instruments that event, and no capture records mode. The next instrument should log window rect + source-bitmap W/H at SetBitmap time, stamped with the current mode.\n\nNot one of the six dead attributions in new words — it is a coverage/null claim, not a mechanism.",
   "refuted": true,
   "wouldBreakIntegerTiers": false
  }
 },
 {
  "key": "v2:6eac9b65ef8993e344c843249633715b116ebea8dcd0dd9f3780f2c91e940b62",
  "result": {
   "refuted": true,
   "reason": "REFUTED as stated. Three quoted numbers are wrong on re-measurement, the core census is a re-derivation of a month-old note in this repo, and the load-bearing conclusion rests on evidence that never touches the variable it claims to settle.\n\nWHAT I RE-MEASURED AND CONFIRMED (scripts in scratchpad: census.py, roots.py, scan.py, scan2.py, instcensus.py):\n- Census over every DBPF in the install tree: group 08000600 = 10 entries, all type 0, all in SimCity_1.dat; group 96a006b0 = 271. Set difference 08000600 - 96a006b0 = EMPTY. Exact match.\n- Exe scan of \"C:\\Program Files (x86)\\Steam\\steamapps\\common\\SimCity 4 Deluxe\\Apps\\SimCity 4.exe\": dword 0x08000600 total=1, aligned=0, at file 0x71E1FF (off%4==3). Context is unambiguously a bit-reversal table, not a constant: ... 08001a00 08009a00 08005a00 0800da00 08003a00 0800ba00 08007a00 0800fa00 08000600 08008600 ... Coincidence confirmed. Positive control 0x96a006b0 = 75 total / 22 aligned, exact. No other BCD-resolution group exists either (0x06400480, 0x10240768, 0x12800960, 0x16001200 etc. = 0 hits), and no ASCII \"08000600\"/\"96a006b0\".\n\nWRONG NUMBERS (rule 1):\n1. Root geometry. Quoted \"08000600 roots span x=140..798, 96A006B0 roots span x=140..1020\". Measured across all 10 twin pairs: 08000600 x-range = 11..839, 96a006b0 (same 10) = 11..1027; across all 271 96a006b0 roots = 0..1154. The quoted figures describe 3 files of 10 (I-2bc90671 / I-898897de / I-aa1f1f57). I-2a2aed99's 800x600 variant is (270,783,839,826) — right edge 839, past 800 — so even the tidy \"800-wide screen\" reading is not clean.\n2. \"strict 10-instance DUPLICATE subset\". Measured: ZERO of the 10 pairs are duplicates. All differ, from 2 changed lines (e99237ff) to 220 (aa1f1f57). It is a subset by instance ID with different geometry — which is what tools\\uiscripts\\UISCRIPTS.md already said.\n3. TIERS line: \"costs 4 wasted entries in each shipped dat\". Measured 10, not 4 — z_SC4UIScale_SelectiveArt-15x.dat in Plugins and SelectiveArt-2x.dat in every dist bundle back to v2.85.0 each carry 10 x group 08000600.\n4. \"all 9 install archives\": there are 11 DBPF .dat files in the install tree (the 9 plus Sku_Data\\Sku1_US and Sku2_European\\Plugin_002_TheLivingMall.dat). Neither carries these groups so the count survives, but this repo has a standing law about exactly this enumeration failure.\n\nNOT NEW (rule: check previous work first): the entire census — 271 / 10 / 48 / 1, \"group ID literally encodes 0800 x 0600\", \"same instance IDs and same window IDs as their 96a006b0 twins, different pixel geometry\" — has been written down since 2026-07-21 in <PROJECT-ROOT> 1 Project\\1 Completed Projects\\SC4UIScale\\tools\\uiscripts\\UISCRIPTS.md. Even the exe-hit method is there (line 118).\n\nTHE INFERENCE IS WEAKER THAN ADVERTISED: I tested \"absent from the exe ⇒ dead\" against the live group itself. 175 of 271 scripts in group 96a006b0 (65%) get ZERO dword hits in the exe. So in this binary, not being named is the normal case for live UI data. (Fair caveat: the claim's null is at GROUP level, where 96a006b0 is named 75 times, so the group-level null is stronger than the instance-level one. But \"never named by the exe\" cannot carry the weight of \"not a live variant at all\" — nobody ran the game at 800x600, which is SC4's own minimum mode, and no probe established what loads there.)\n\nTHE HEADLINE IS UNSUPPORTED WHERE IT MATTERS: \"the live group is 0x96A006B0 in EVERY MODE.\" Nothing offered — census, root x-spans, byte scan — touches modes. The mode/variant axis was never at group level. The repo already put it at INSTANCE level and already resolved it: composite HUD window 0xE9889775 has one live script, G-96a006b0\\I-2bc90671, with I-898897de a stale dev copy refuted by live dump geometry (src\\UiSpike.cpp:5741-5746 — \"898897de predicts 407/417 and is refuted\"; UISCRIPTS.md line 110). ONE script serves mayor and god mode.\n\nUSEFUL BY-PRODUCT — the prompt's named gap closes positively, not by null. The DRAWPROBE rect (180,85) 153x17 equals floor(1.5 x (120,57)) and 1.5 x 102x11. Area (120,57,222,68) for id 0x8a517556 exists in exactly ONE file: extracted\\T-00000000_G-96a006b0_I-2bc90671.ui line 22. The alternatives are ruled out arithmetically: 898897de is (130,57) -> (195,85); 08000600\\2bc90671 is (359,144) -> (538,216); 08000600\\898897de is (163,144) -> (244,216). So that capture was the LIVE composite HUD's bar, already 153 wide — and because one script serves both modes, the lead's \"wrong root list / a different parent than in god mode\" branch has no separate mayor-mode script to hide behind. That branch should be narrowed to a runtime question (does the sweep re-run / get gated on the mode switch for the SAME tree), not a data question.\n\nKILLER TEST (rule 2): passes only by being inert — the claim is explicitly tier-symmetric and self-declares it \"cannot change what renders\". Rule 5: it predicts nothing resembling a 1/3-to-1/2 fill loss; it predicts no visual difference at all. It is not one of the six dead attributions (rule 3), and its null does carry a real positive control (rule 4) — but a correct null on the wrong variable.\n\nWHAT SURVIVES: the narrow, already-documented fact that group 0x08000600 is the 800x600 layout set and is not what renders at any resolution this mod's tiers admit. Stop measuring those two files. That is all this run bought, and it was already on disk.",
   "wouldBreakIntegerTiers": false
  }
 },
 {
  "key": "v2:fd5806bad98d2c4c76de55d2dd10ee8d216a1f449e035b1dc2dc0dd12c998b22",
  "result": {
   "refuted": true,
   "wouldBreakIntegerTiers": true,
   "reason": "REFUTED — the construct is a for-loop that builds ALL FIVE, not a mode selector. The half of the claim that is a bare identification survives and is worth keeping; the headline is false.\n\nWHAT I RE-MEASURED AND CONFIRMED (exe: C:\\Program Files (x86)\\Steam\\steamapps\\common\\SimCity 4 Deluxe\\Apps\\SimCity 4.exe, ImageBase 0x400000, .text file-offset == RVA so VA-0x400000 is a valid mapping):\n- Jump table at 0x7EF1D8 = {0x7EE230, 0x7EE248, 0x7EE254, 0x7EE260, 0x7EE23C} — exactly as claimed, and referenced from exactly one site (0x7EE22C).\n- Case bodies exactly as claimed: idx0 0x69E3D347/0xC991EDA8, idx1 0x2BC90671/0x69E40A1F, idx2 0xAA1F1F57/0xCA1F1D9C, idx3 0x49889894/0xC98F49F1, idx4 0xAA53E3EA/0x0A78827A. `xor eax,eax; mov [esp+0x10],eax` at 0x7EE214/0x7EE216.\n- Byte-scan counts exactly as claimed, positive controls intact: 0x898897DE = 0; 0x2BC90671 = 1 (@0x7EE249); 0xE9889775 = 7; 0x8A517556 = 1 (@0x7E86E5).\n- Log C:\\...\\SC4UIScale\\_tests\\captures\\SC4UIScale-2026-08-16-174827.log lines 468/489 verbatim: RCI columns at x=394/409/424. Design x in G-96a006b0_I-2bc90671 = 263/273/283 (area 263,56,271,127); in G-96a006b0_I-898897de = 278/288/298. Corner-truncated x1.5 gives 394/409/424 vs 417/432/447 — the live dashboard IS 2bc90671. Confirmed a SECOND, independent way: the bar 0x8A517556 is at design (120,57,222,68) in 2bc90671 and (130,57,232,68) in 898897de; the DRAWPROBE at log line 497/1282 reads (180,85 153x17), which is 120/57/222/68 x1.5 exactly. 898897de would land at x=195. Identification of the live script is solid.\n\nWHY THE CLAIM IS STILL REFUTED:\n1. WRONG ADDRESS. The `cmp eax,4 / ja / jmp [eax*4+0x7EF1D8]` head is at 0x7EE220-0x7EE229. VA 0x7EE241 is `mov ebp, 0xA78827A` — the middle of case idx4. The quoted number is wrong by 0x21.\n2. IT IS NOT A SELECTOR, IT IS A LOOP — the claim's own evidence concedes this (\"eax being a 0..4 loop counter\"). Measured back edge at 0x7EE610: `inc eax / cmp eax,5 / mov [esp+0x10],eax / jb 0x7EE220`. The `ja default` target is 0x7EE610, i.e. the default case IS the loop increment. The only writes to the counter slot [esp+0x10] in the whole block are the `xor eax,eax` at 0x7EE216 and the `inc` at 0x7EE610; nothing reads a mode variable anywhere in 0x7EE220-0x7EE618, and there is no inbound reference to 0x7EE220 other than that back edge. All five script+root pairs are constructed on every pass, in order 0,1,2,3,4, into five parallel slot arrays ([esi+0x1AC+12i] and [esi+0x384+12i]). A construct that runs all five branches selects nothing. \"The city-HUD mode selector is a 5-case switch in the exe\" is false.\n3. THE MAYOR-vs-GOD ASSIGNMENT IS A COMMENT, NOT A MEASUREMENT, AND THE FILE CONTRADICTS ITSELF. <PROJECT-ROOT> 1 Project\\1 Completed Projects\\SC4UIScale\\src\\UiSpike.cpp:4973 places 0x69E40A1F — the root the claim assigns to \"mayor\" — inside the array `kGodPanelIds`, labelled \"god/mayor left-toolbar stock-layout panel\", and the comment above it states \"0x69E40A1F reports vis=0 in god mode while its children still draw\". Line 5332 labels the same id \"mayor toolbar column\". The claim quotes only the second. Live corroboration against the loop, not the selector: in ONE capture, log lines 403/409/411/436 scale 0x0A78827A (idx4 root), 0x69E40A1F (idx1 root), 0xC991EDA8 (idx0 root) and 0xABB26B0E simultaneously. A mode selector predicts one; a build-all-five loop predicts all of them. So \"god mode uses entirely DIFFERENT scripts\" is not established by this evidence.\n4. \"ZERO REFS ⇒ DEAD\" IS AN INVALID INFERENCE HERE. Group 0x08000600 also has zero genuine dword references in the exe — my scan's single hit at 0x00B1E1FF is a misaligned overlap inside a .data table of `00 XX 00 08` 16-bit entries, not a standalone dword — yet the G-08000600 twin of the LIVE 0x2BC90671 exists in the DAT with a wholly different horizontal layout (bar at 359,144,461,155; RCI columns 111,152,182,156 = 71x4). By the claim's own rule that twin would be \"dead\" too. Also, no .ui script in the 331-file extraction names 898897de or 2bc90671 as a child/clone, so there is no positive control for what a live cross-script reference would even look like. The defensible statement is the narrow one: the live 1.5x dashboard is G-96a006b0_I-2bc90671, proven twice on geometry — not \"898897de is a dead pre-Rush-Hour twin\". (Its \"hard-coded captions\" sub-claim does check out: 21 '##########' placeholders in 2bc90671 vs 0 in 898897de.)\n5. IT CANNOT BE THE DEFECT AND PREDICTS NOTHING ABOUT FILL LENGTH. The claim states mode selection is tier-independent, so it applies identically at 2x/3x where the bar is user-confirmed correct — the killer test kills it as a causal mechanism. It also predicts no fill fraction at all, gross or sub-pixel.\n6. IT ARGUES AGAINST THE HYPOTHESIS IT WAS RECRUITED FOR. In the very log it cites, 0xE9889775 is swept (line 434: (139,1413 880x180) -> (209,1319 1320x270), exactly x1.5) and the bar window measures 153x17 = exactly 1.5x its 102x11 design. The \"root not swept\" story has no support here.\n\nUSEFUL RESIDUE FOR THE PARENT (not part of the claim): the same log gives the first real mode evidence — line 508 at 17:48:05 \"mayor flyout 0x699306ED at(16,658) size 173x533, +10 win (docked)\" precedes the second DRAWPROBE of 0x8A517556 at 17:48:13.559 (line 1282), same rect (180,85 153x17). That is a mayor-mode-adjacent capture showing the window correctly scaled — a lead, not proof, and it points away from a window/sweep cause."
  }
 },
 {
  "key": "v2:6515bcab69433bb37b22032eed447c693eae7eec14d2a0326e1aa8cec4e52415",
  "result": {
   "lane": "GZWinBMP draw-path arithmetic + supplier/parent clip audit for HUD Mayor Rating bar 0x8A517556 — what clips the drawn bar to ~2/3 at 1.5x but not at 2x, assuming the window really is 153x17 in both modes",
   "findings": [
    {
     "claim": "THE BLIT NEVER READS THE WINDOW'S SIZE — ONLY ITS ORIGIN. GZWinBMP Plot 0x9BC325 plain path is dst = { absArea.l, absArea.t, absArea.l + srcW, absArea.t + srcH }, where the source rect is the STORED rect at [win+0xE8] whenever holder flag 0x10 (has imagerect) or 0x20 (1:1) is set — and the ctor writes flags 0x12 at [win+0xF8], so 0x10 is set by default and the stored rect is ALWAYS the source of truth. Only if both bits are clear does it fall back to image->vt[0x30] (the image's own rect). Flag 8 diverts to the 9-slice path (src.r/=3, src.b/=3, helper 0x8D8800). CONSEQUENCE: the destination can never be 'clipped by the window' — it is exactly the size of the stored source rect, wherever that came from. So the 153-into-102 question has only one possible answer: the STORED RECT was 102.",
     "evidence": "Full disasm of 0x9BC325-0x9BC446 (probe asserts vt[0x00ADF6A0]+88*4 == 0x009BC325 first). 0x9BC3B9-0x9BC3D4: eax=[ebx+0x24] (abs area l), [ebp-0x20]=eax, eax-=src.l, eax+=src.r, [ebp-0x18]=eax — i.e. dst.r = area.l + (src.r-src.l). Same for top/bottom. 0x9BC36F-0x9BC39E: holder(this+0xd8)->vt[0x28](0x10) then (0x20) select esi=this+0xE8, else image->vt[0x30]; 4x movsd copies that rect to the local. 0x9BC3F6-0x9BC409: ctx=[ebx+0x6c]; ctx->vt[0x98](image,&src,&dst). Ctor 0x9BC4BA zeroes [esi+0xE8..0xF4] and sets [esi+0xF8]=0x12; the flag test 0x9BC171 reads [iface+0x20] = win+0xF8.",
     "tierSignature": "tier-independent — no factor, no rounding, no division anywhere in the plain path",
     "confidence": "measured"
    },
    {
     "claim": "[win+0xE8] IS A LATCH, NOT THE .UI CROP — AND SetArea NEVER REFRESHES IT. The only writer is 0x9BC447: srcRect = {0, 0, min(GetArea().w, img->GetWidth()), min(GetArea().h, img->GetHeight())}, and it has EXACTLY TWO callers in the whole image (0x9BC56E, 0x9BC5B8), both inside the cIGZWinBMP SetImage/SetBitmap pair (iface vtable 0x00ADF66C slot 4 = 0x9BC57E, the one the controller calls at 0x7E8700). GZWinBMP's SetArea override (window vt slot 55 = 0x9BC0B8) is a bare `jmp 0x99C837`; that worker stores the new area at +0xA8, reallocates the private buffer when the size changed, calls CalcAbsoluteArea (which is what updates the +0x24 Plot reads) — and never touches +0xE8. THEREFORE: resizing a GZWinBMP after its bitmap was supplied enlarges the FRAME and not the DRAWING, permanently, until the next SetBitmap. This is the one and only way a 153-wide source is drawn 102 wide, and it also means the pre-scaled `imagerect=(0,0,153,17)` we ship for this widget is DEAD DATA — overwritten at the first rating update.",
     "evidence": "0x9BC447: edx=arg.r-arg.l, ecx=arg.b-arg.t; [esi+0xE8]=0,[+0xEC]=0,[+0xF0]=edx,[+0xF4]=ecx; then edi=&[esi+0xF0], image->vt[0x24] (GetWidth), `cmp [edi],eax / jle` -> clamp, and the same for +0xF4 vs vt[0x28] (GetHeight). SetBitmap 0x9BC57E: AddRef new, Release old, [esi+4]=new (= win+0xDC), `add esi,0xFFFFFF28` (-0xD8 -> the window), `call [vt+0xC0]` = 0x99BCE1 = `lea eax,[ecx+0xA8]; ret` (GetArea), then `call 0x9BC447`, then [vt+0x170] invalidate. SetArea worker 0x99C837 writes only +0xA8..+0xB4, calls [vt+0x190] PrivateBuffer(1) if ([esi+0xCA]&1) and the size changed, then [vt+0x168] CalcAbsoluteArea. Direct-caller scan of .text with a POSITIVE CONTROL (target 0x8D8800 returned 6 callers incl. the known 0x9BC439) gives 0x9BC447 -> exactly 2 callers.",
     "tierSignature": "mechanism tier-independent; the DAMAGE is 102/153=66.7% at 1.5x, 102/204=50% at 2x, 102/306=33% at 3x, 0% at 1x",
     "confidence": "measured"
    },
    {
     "claim": "THE 1.5x SESSION LATCHED THE CROP AT 1x GEOMETRY AND NEVER RE-LATCHED; THE 2x SESSION RE-LATCHED AFTER THE SWEEP. That is the entire tier split, and it is a RACE, not a law — 2x is only ACCIDENTALLY correct. 1.5x capture: the controller ran ONCE (17:47:00.928) while the subtree was still 1x, so the crop latched to min(102,153) x min(11,39) = 102x11; the city sweep enlarged the HUD 0.8 s later (17:47:01.737); both later paints show the window at 153x17 with no further controller call in 75 s — the bar draws 102x11 at the window's top-left = 66.7% of the width and 64.7% of the height, and the missing right-hand third is exactly where the green (positive-rating) ticks live. 2x capture: the controller ran TWICE — once pre-sweep (17:19:49.296) and again at 17:19:54.496 AFTER the sweep, which re-ran SetBitmap and re-latched the crop to min(204,204) x min(22,52) = 204x22 = full.",
     "evidence": "_tests/captures/SC4UIScale-2026-08-16-174827.log (1.5x): l.222 RATEANCHOR ctl=03173014 arrow L/T=(98,58) 21x9 step=11 — 1x coords, 1x height; l.372/434-435 `panel 0xE9889775 (139,1413 880x180) -> (209,1319 1320x270)` + `city panel 0xE9889775 - 30 windows scaled` (exactly x1.5); l.497/1282 DRAWPROBE win=0x8A517556 rect=(180,85 153x17); RATEANCHOR appears exactly twice in the file (one is the install line). SC4UIScale-2026-08-16-172018.log (2x): l.221 RATEANCHOR arrow (98,58) 21x9 -> l.365 sweep -> l.551 DRAWPROBE 204x22 -> l.630 RATEANCHOR arrow (196,116) 2x18 (2x coords, 2x height) -> l.631 DRAWPROBE 204x22. The SAME log line printing 1x coords at one call and 2x coords at another is the self-control for 'what geometry was live at that call'.",
     "tierSignature": "1x 100% / 1.5x 66.7% wide & 64.7% tall (measured order) / 2x healed by a second controller tick (measured) / 3x would be 33% under the same race — the arithmetic does NOT go silent at 2x, the SESSION did",
     "confidence": "measured"
    },
    {
     "claim": "ALL THREE ART/CLIP SUPPLY THEORIES ARE REJECTED BY MEASUREMENT. (a) The {1ABE787D,14015549} twin is never used: the controller pushes group 0x46A006B0 as an immediate at 0x7E8521 and instance 0x14015549 at 0x7E851C into the loader 0x602B70 — the .UI binds the same TGI — and our deployed 15x dat WINS that TGI at 153x39. The 1ABE787D twin is stock 102x26 and is overridden by NO tier of our packages, so if it were the source 2x and 3x would be worse, not silent. (b) No parent clips the child: the declaring root 0xE9889775 is swept 880x180 -> 1320x270 (exactly x1.5) in the same capture, and the bar's 153-wide window sits at design x=120..222 inside an 880-wide design root. (c) The private buffer is not stale: SetArea's worker calls PrivateBuffer(true) whenever the size changed and the pbuff flag [win+0xCA]&1 is set — and this window is winflag_pbuff=yes — so the #47 gauge pbuff-clip mechanism cannot be active on a swept window.",
     "evidence": "Byte scan of the exe (positive control: the known code-bound mayor-face TGI 0x14315E60 found at 0x007E8AF5) -> 0x14015549 occurs at exactly 0x007E851D and 0x007ED224; disasm at 0x7E851C-0x7E852F shows `push 0x14015549 / push 0x46a006b0 / call 0x602b70`. DBPF payload read with a stated control (stock {46A006B0,14015549} must read a real WxH — it read PNG 102x26): GAME/SimCity_1.dat 46A006B0=102x26 and 1ABE787D=102x26; PLUGINS-ROOT/z_SC4UIScale_SelectiveArt-15x.dat 46A006B0=153x39 and NO 1ABE787D entry; the 2x repo dat likewise carries only 46A006B0 (204x52). who_owns_tgi.py: winner = z_SC4UIScale_SelectiveArt-15x.dat. Sweep line l.434 of the 1.5x capture for the parent; 0x99C837 disasm for the pbuff realloc.",
     "tierSignature": "all three are tier-INDEPENDENT and would break 2x/3x at least as badly — rejected by the 2x-must-stay-silent test as well as directly",
     "confidence": "measured"
    },
    {
     "claim": "THE CURE IS ALREADY WRITTEN AND IS SIMPLY OUT OF SCOPE: BmpCtxBltThunk fixes this exact shape. The stale latch produces dst size == src size (102x11 == 102x11), which is precisely the follows-source signature the thunk accepts; with gBmpScale=1.5 and the live window 153x17 it computes dw=round(102*1.5)=153 and dh=round(11*1.5)=17 — exactly the window, with both overflow clamps inactive. When the latch is already correct (153x17 in a 153x17 window) m clamps to 1.0 and the thunk is a no-op, so it is self-limiting at every tier. The 1.5x capture lists 11 BMPX-hooked roots (0x698894D3, 0xCA1F1D9C, 0xAA1F1EC5, 0xEA1F1E4D, 0xEA1F1E4E, 0xEA1F1E5E, 0x6A61E29F, 0xABBAA2D3, 0x8A8B5B71/72, 0x0A4A8176) and 0xE9889775 — the composite status HUD that declares this bar — is NOT among them. The alternative, more general cure is to re-issue SetBitmap([win+0xDC]) through iface vt 0x00ADF66C slot 4 after any sweep resize of a class-0x00ADF6A0 window, which re-runs 0x9BC447 against the new area (SetBitmap AddRefs the new image before Releasing the old, so re-setting the same pointer is safe).",
     "evidence": "src/UiSpike.cpp:8738-8800 BmpCtxBltThunk (w==sw && h==sh gate, float m=gBmpScale, both clamps only fire when the scaled bitmap is TOO BIG, dst[2]=dst[0]+dw). SetBitmap AddRef/Release order read at 0x9BC58B-0x9BC5A2. BMPX scope from the 1.5x capture's own `BMPX N instance(s) hooked under 0x…` lines; the four declaring scripts (T-0/G-08000600|96a006b0/I-2bc90671|898897de) all have root id 0xE9889775, and UiSpike.cpp:5741 records the LIVE script as G-96a006b0_I-2bc90671 (880x180 root), matching the swept 880x180 -> 1320x270.",
     "tierSignature": "cure is exact at 1.5x (153x17), exact at 2x (204x22) and 3x (306x33), and a no-op at 1x and whenever the latch is already current",
     "confidence": "derived"
    }
   ],
   "positiveControl": "Four controls, each stated before its result. (1) DISASSEMBLER: the probe asserts vt[0x00ADF6A0]+88*4 reads 0x009BC325 AND that 0x007E87B1 contains 6B F6 07, and refuses to print anything otherwise — both PASSED, so every VA quoted maps to real bytes. (2) CALLER SCAN: before believing '0x9BC447 has exactly 2 callers', the same scanner was run on 0x8D8800, whose caller 0x9BC439 is already documented — it returned 6 callers including that one, so a 2 is a count and not a refusal (a zero-caller answer would have been a refusal). (3) DBPF PAYLOAD READ: the reader had to report a real WxH for the stock {46A006B0,14015549} in SimCity_1.dat before any 'not overridden' claim was allowed — it read PNG 102x26, and the byte-scan control found the known code-bound mayor-face TGI 0x14315E60 at 0x007E8AF5, so 'no 1ABE787D entry in our dats' is an absence the instrument could have seen. (4) LIVE LOG: the capture contains the DRAWPROBE's own control line ('slot 88 paint #1 observed … if no DRAWPROBE win= rows follow, that is an ANSWER, not a dead probe') and 26 subsequent rows including this window; and the geometry-at-call-time reading is self-controlled because the SAME RATEANCHOR line format printed 1x arrow coords (98,58) 21x9 in one call and 2x coords (196,116) 2x18 in another.",
   "gaps": "1. NO INSTRUMENT EVER SAW THE DRAWN RECT for this window. The 102x11 draw is DERIVED from the code plus the measured call order — BMPX does not cover root 0xE9889775, so no src/dst line exists for 0x8A517556 in any capture. The one-line settlement: dump [win+0xE8] (the latched src rect) for 0x8A517556 at paint, or extend BmpDrawThunk's scope to 0xE9889775 and read its existing SEATPROBE/BMPX row — if it prints 'img 102x11 win 153x17 -> dst 153x17 (x1.50)', this is confirmed AND cured in the same build. Until then finding 3's on-screen consequence is a hypothesis about the symptom, not an observation of it.\\n2. MODE IS STILL UNESTABLISHED. I did not determine whether the 17:47 (1.5x) or 17:19 (2x) captures were mayor mode or god mode, so the user's 'it is in MAYOR MODE' is not corroborated by me. The mechanism is mode-independent (one controller, one window id, one latch), so I do not think it matters — but that is reasoning, not measurement.\\n3. SAMPLE SIZE ON THE RACE. n=2 captures at 1.5x (both show a single controller call) and n=1 at 2x (which shows two). Whether a 1.5x session ever gets a second rating tick — which would make the bar heal itself mid-session and would be a strong confirmation if the user ever sees it snap to full — is unmeasured.\\n4. VTABLE-DISPATCHED WRITERS ARE INVISIBLE to my caller scan; it finds only direct E8/E9 calls. Another site could reach SetImage/SetBitmap through the interface vtable (0x00ADF66C slot 4) without appearing in my count of two.\\n5. I did not identify window vt slot 62 (+0xF8 -> 0x9BC251) or slot 68 (+0x110 -> 0x99DB6B), the other two GZWinBMP overrides; neither writes +0xE8 on the path I read, but I did not disassemble them end to end."
  }
 },
 {
  "key": "v2:6b0a8aeab6cce78b253af02d8fdf7c1804d37f96ace7b7d620d9fd6f7e30e373",
  "result": {
   "refuted": false,
   "reason": "NOT REFUTED. I re-measured every quoted number and all of them hold, and the one real gap the brief named (mode unestablished) is now closed by evidence the claim itself did not cite.\n\nRE-MEASURED, ALL CORRECT: _tests/captures/SC4UIScale-2026-08-16-153245.log:496 = win=0x8A517556 rect=(180,85 153x17) with the 'DRAWPROBE live' positive control at :470; SC4UIScale-2026-08-16-174827.log:497 and :1282 = same rect (control :471); SC4UIScale-2026-08-16-172018.log:551,631 = rect=(240,114 204x22) (control :476). Tier lines at :2 of each log confirm ScaleFactor=1.50 / 1.50 / 2.00. Design (120,57) 102x11 at tools/uiscripts/extracted/T-00000000_G-96a006b0_I-2bc90671.ui:22. Shipped crops read off disk: stage-15x (0,0,153,17), stage (0,0,204,22), stage-3x (0,0,306,33). PNG IHDRs decoded from the dats: 15x 153x39 @0x00373C51, 3x 306x78, 2x 204x52 (tools/selective-safe/z_SC4UIScale_SelectiveArt.dat @0x002B97FE), and the DEPLOYED copy _working-backup/PRE-OVERNIGHT-2026-08-15-2145/DEPLOYED-Plugins/z_SC4UIScale_SelectiveArt-15x.dat is also 153x39 -- exact NN of 102x26 on both axes, no CellUnit snap. Not one number is wrong.\n\nTHE MODE GAP IS CLOSED, IN THE CLAIM'S FAVOUR. VWKID enumerates only VISIBLE direct view children (src/UiSpike.cpp:15889-15919, `if (!w || !w->IsVisible()) continue`). Each 1.5x DRAWPROBE row is bracketed within ~50 ms by a full visible-set dump containing 0x69E40A1F (mayor toolbar column) and 0xE9889775 at (209,1319 1320x270), with the god toolbar cluster 0xC991EDA8 and the god-mode panel 0x0A78827A ABSENT: 174827:499-506 (probe at :497) and 174827:1284-1291 (probe at :1282); 153245:498-505 (probe at :496). The 2x probe has the identical bracket at 172018:553-560 with the HUD at (278,1226 1760x360). So 1.5x and 2x were both measured in MAYOR MODE, symmetrically. God mode only enters that session at 174827:1462 (VWKID 18 = 0x0A78827A), after the last rating-bar reading. 1320=880*1.5, 270=180*1.5, 1760=880*2, 360=180*2.\n\nKILLER TEST KILLS THE LEAD, NOT THE CLAIM. The lead's mechanism (wrong root list / mode gate / early return / kNeverScale / different parent) is TIER-INDEPENDENT. A sweep that misses this window in mayor mode misses it at 2x as well, predicting 102/204 = 50% fill -- grosser than the 1.5x symptom. 2x is user-confirmed correct AND measured at 204x22 in the same mode. The lead is self-refuting.\n\nTHE SWEEP IS DIRECTLY OBSERVED REACHING IT. SC4UIScale-2026-08-15-180919.log (ScaleFactor=1.50, :2) dumps the tree pre-sweep at :1195-1225 with 0xE9889775 at 880x180 and 0x8A517556 at pos(120,57) size(102x11); 7 ms later :1343 `panel 0xE9889775 (139,1413 880x180) -> (209,1319 1320x270)` and :1344 `city panel 0xE9889775 - 30 windows scaled`. I counted the descendants in that dump: exactly 29, plus the root = 30. Nothing skipped. Also confirmed 0xE9889775 is in the born-2x pre-scale list (UiSpike.cpp:5334) and NOT in kDataScaledSubtreeIds (5410ff), so ScaleSubtree does recurse -- law 96 does not bite here.\n\nCHECKS 3 AND 4: not one of the six dead attributions in new words -- it is dead attribution #5's own refutation, strengthened with the 2x control and a mayor-mode bracket. Not a null: the positive control is present in all three runs, and the probe budget was NOT exhausted (DRAWPROBE rows continue to 174827:1892 of 1910 lines), so the sample is not truncated.\n\nTWO PRECISION CORRECTIONS THAT DO NOT OVERTURN IT. (1) 'Never 102 wide at 1.5x' is literally false for the few ms before the sweep -- 180919.log:1216 shows exactly 102x11 at ScaleFactor=1.50 -- but that transient exists identically at 2x, so it cannot be the defect. Reword to 'never 102 wide once the sweep has run'. (2) The claim's stated sizing path is only half-verified: measured t=85 is not RoundHalfUp(57*1.5)=86, and sibling 0xC988BC79 rounds l=97*1.5=145.5 DOWN to 145 while t=37*1.5=55.5 goes UP to 56, so position is not following the rule the claim's narration implies. The SIZE (153x17) is measured directly, so this is looseness in the mechanism story, not in the conclusion. Note also the three 2026-08-03 DPROBE rows reading 153x16 (one row short of the 17 crop) -- an OLD build, and 153 wide either way, so it does not restore the lead.\n\nBOTTOM LINE: window == crop at 1x/1.5x/2x/3x, measured in mayor mode at both 1.5x and 2x. The lead ('the window stays 102 wide') is dead. The defect is elsewhere -- the box is right, so look at what FILLS it (the controller's runtime buffer at 0x7E8510/0x7E8700 and what the dst-follows-src draw does with a 153x39 source under a 17-row crop).",
   "wouldBreakIntegerTiers": false
  }
 },
 {
  "key": "v2:017ccef27835c4d7334854dbd39063e3814129c532cf0a65594315b1f598e4e1",
  "result": {
   "refuted": false,
   "wouldBreakIntegerTiers": false,
   "reason": "NOT REFUTED. I tried to kill this and could not. Every load-bearing number re-measured true, and I closed two gaps the claim left open — both in its favour. Two factual errors found, neither load-bearing.\n\n=== 1. MEMBERSHIP AUDIT RE-RUN INDEPENDENTLY (programmatic extraction of every id array in src/UiSpike.cpp) ===\n0xE9889775 IS a member of kAlwaysScaleCityIds (:5271) at :5334 \"composite status panel\" and of kCityHudFamilyIds (:5755) at :5756 \"<-- LEADER\". It is NOT in kNeverScaleIds (:4815), kDataScaledSubtreeIds (:5410), kCityDialogIds (:14751), kGodToolFlyoutIds (:4936), kGodPanelIds (:4972), kRegionPanelIds (:4781), kSubFlyoutIds (:5248, which is `= { 0x8A6E61E0 };` — one member), kFontSizedIds (:5542), kAdviceListNeverTouchIds (:5639), kAdviceListScaleSelfIds (:5602), kOwnsBackgroundSheet (:14462). FindMayorDock (:5205) has no 0xE9889775 entry. All quoted line numbers land exactly. The sweep's only visibility gate is src/UiSpike.cpp:10310-10316 and IsAlwaysScaleCityId is an explicit exception there. `mayorModeActive` exists at exactly one site (:13016) and gates ScaleGodFlyouts' flyout docking (:13669, :13832), never the generic panel loop.\n\n=== 2. TWO ERRORS IN THE CLAIM (real, non-load-bearing) ===\n(a) \"sits in exactly ONE list\" is FALSE and self-contradicted by its own next sentence: it is in TWO. kCityHudFamilyIds is a positioning family, not a gate, so the conclusion stands — but the number is wrong.\n(b) \"0x8A517556 itself is in NO list at all\" is FALSE. It IS in kDrawWatch at src/UiSpike.cpp:3483 (\"HUD Mayor Rating bar (GZWinBMP, vt already patched)\"). Probe-only, zero scaling effect — but the enumeration was presented as exhaustive and missed a list.\n(c) \"the bar is already 153x17 when mayor mode first paints it\" — the MODE was asserted, never shown. That was the stated gap and the claim did not close it. I closed it below.\n\n=== 3. GAPS I CLOSED (the claim assumed these) ===\nPARENTAGE, previously assumed: 0x8A517556 is a depth-2 GRANDCHILD of 0xE9889775 via an id-less GZWinBMP. Verified by parsing tools/uiscripts/extracted/T-00000000_G-08000600_I-898897de.ui and _I-2bc90671.ui — root 0xE9889775 (GZWinGen) > unnamed GZWinBMP > 0x8a517556.\nNOTHING TRUNCATES THE RECURSION, and the claim only checked the id-list axis: ScaleSubtree (src/UiSpike.cpp:17161) has NO visibility gate and NO clsid gate on children; kMaxDepth=8 (:4758) vs the bar at depth 2; kMaxWindows=1500 (:4759) vs 504 total scaled; AlreadyScaled still recurses (:17440); centerThisLeaf cannot fire (bar is 102x11, spikeCenterLeafMaxPx defaults 48 in Settings.h:126 and spikeCenterSmallLeaves defaults false, and the city call passes centerLeaves=false at :7418).\nSINGLE-SWEEP PROOF: the panel line is gated on n>0 (:10362), and \"panel 0xE9889775\" occurs exactly once in the whole launch. So all 30 mutations happened in the one event at 17:47:01.741 — while hidden. This is the strongest evidence in the claim and it did not spell it out.\nMODE, established from the VWKID timeline (VWKID at :15896-15919 filters on IsVisible() and re-dumps the whole visible set on signature change, so absence = not visible): god-mode interval 17:48:09.658–17:48:13.247 (0xE9889775 ABSENT, 0xC98F49F1 present, 0x69E40A1F absent — and :4965 says 0x69E40A1F reports vis=0 in god mode); Sim-mode interval from 17:48:14.417 (0x698894D3 / 0xCA1F1D9C / 0xABB26B0E present, HUD absent). BOTH 0x8A517556 paints — :497 17:47:04.718 and :1282 17:48:13.559 — sit in mayor-mode intervals, each ~50ms before a VWKID dump showing the HUD visible, and NEITHER falls inside the god-mode window. Mayor mode confirmed. Rect identical both times: (180,85 153x17).\n\n=== 4. DECISIVE NEW EVIDENCE THE CLAIM DID NOT HAVE ===\nThe same widget measured at the 2x tier: _tests/captures/SC4UIScale-2026-08-16-172018.log:551 and :631 — `DRAWPROBE win=0x8A517556 rect=(240,114 204x22)`. That is EXACTLY 2x of design (120,57 102x11). Against three 1.5x readings of (180,85 153x17) = exactly 1.5x (172018 is 2x; 153245:496 and 174827:497/:1282 are 1.5x). The window scales proportionally at BOTH tiers. Since 2x is user-confirmed correct on screen, the window rect cannot be the discriminator between the tiers. THE LEAD IS DEAD: \"the window stays 102 wide while the art and crop are 153\" is measured FALSE in mayor mode.\n\n=== 5. TIER TABLE RE-MEASURED, ALL EXACT ===\n1.5x _tests/captures/SC4UIScale-2026-08-16-174827.log:434 (139,1413 880x180)->(209,1319 1320x270); 2x _tests/captures/2026-07-30-border-hunt-vistrace.log:234 and 2026-07-31-task89-ours-baseline-SC4UIScale.log:242 ->(278,1226 1760x360); 3x _tests/captures/SC4UIScale-2026-08-04-140015.log:257 (139,1973 880x180)->(417,1599 2640x540). \"30 windows scaled\" is IDENTICAL at all three tiers across ~20 captures. (I initially could not find the 3x row and was ready to call it computed-not-measured; it is measured. Retracted.)\n\n=== 6. THE FIVE TESTS ===\n(2) KILLER TEST — would it break 2x/3x? No, and structurally cannot: this is a negative/structural claim, not a defect mechanism. The identical unconditional path runs at every tier (same 30-window count, same exact-multiple root rect at 1.5x/2x/3x). Nothing goes silent at an integer factor. Survives.\n(3) Not one of the six dead attributions — it is the direct structural refutation of THE LEAD, not smoothing / CellUnit / cSC4WinAuraBar / imul-7 / \"window or crop is wrong\" / ragged ticks.\n(4) It IS a null, but with real positive controls: the lists are non-empty and demonstrably capable of catching this id class (0x69E40A1F appears in BOTH kGodPanelIds and kAlwaysScaleCityIds); the n>0-gated sweep line fired; the DRAWPROBE heartbeat positive control is at 174827:471. Passes.\n(5) It predicts no fill mechanism because it proposes none — correct for a refutation. Its consequence is that the mode-gate lead is closed.\n\n=== 7. WHERE THIS LEAVES THE DEFECT (redirect, not part of the verdict) ===\nThe window is right at both tiers, so the fill length must be on the SOURCE side, inside sub_7E8510: v=clamp(rating+100,0,200); H=src->GetHeight; row=(H*v)/200. H is 26 at 1x, 39 at 1.5x, 52 at 2x. At 2x the 26-row filmstrip maps onto 52 buffer rows at an exact 2 rows per state; at 1.5x, 39/26 = 1.5 rows per state, so the row index (39*v)/200 lands on a half-row boundary for odd states and the runtime replication loop samples a row the filmstrip never intended. That is a source-side, integer-exact-at-2x-and-3x mechanism and it is the axis to measure next. Note also CodePatches.cpp:3893-4062 (RATEANCHOR) already hooks sub_7E8510 for the decline arrow and knows kDeclineStepVa=0x7E8A02 — a live instrument already sitting on the exact function."
  }
 },
 {
  "key": "v2:67950f23d611cbeeee9617a669f278d983c918a9d80995d41a88e6812d4ba1a5",
  "result": {
   "refuted": true,
   "reason": "REFUTED — the conclusion is not supported by the evidence given, and the load-bearing inference is falsified by the same two logs the claim cites.\n\n**1. Two quoted numbers are wrong.**\n(a) \"281 extracted .UI scripts\" — actual count in `<PROJECT-ROOT> 1 Project\\1 Completed Projects\\SC4UIScale\\tools\\uiscripts\\extracted` is **330 .ui files** (331 entries incl. `extract-manifest.csv`). Off by 49.\n(b) \"Only 0x2BC90671 declares roots 0xE9889775 + 0x69E40A1F\" — **FALSE**. `grep -ril e9889775` and `69e40a1f` each return the SAME four files: `T-00000000_G-08000600_I-2bc90671.ui`, `T-00000000_G-08000600_I-898897de.ui`, `T-00000000_G-96a006b0_I-2bc90671.ui`, `T-00000000_G-96a006b0_I-898897de.ui`. The so-called \"dead twin\" 0x898897DE declares BOTH roots, in BOTH groups. The exclusivity the discrimination argument rests on does not exist.\n(c) The 4 hits are 2 GROUPS x 2 INSTANCES with four different areas. The live window matches ONLY `T-00000000_G-96a006b0_I-2bc90671.ui` area=(120,57,222,68): 120*1.5=180, 57*1.5=85, 102*1.5=153, 11*1.5=17 → the logged (180,85 153x17); and 120*2=240, 57*2=114 → the logged (240,114 204x22). \"Instance 2bc90671\" alone does not identify the live script; the 08000600 twin at (359,144) is a different window.\n\n**2. The load-bearing inference is measurably false.** The claim says: \"The probe line ... is a PAINT-time hook, so the window was visible on screen at that moment.\" Measured against the project's own visible-set instrument (VWKID — change-only dump of DIRECT view children with `IsVisible()`, `src\\UiSpike.cpp:15889-15919`):\n- 1.5x sample #1: DRAWPROBE 17:47:04.718 (`_tests\\captures\\SC4UIScale-2026-08-16-174827.log:497`). The immediately PRECEDING census, 17:47:01.859 (lines 480-483), contains only {6A5E44B6, AA32BCE6, EA8CAD14, 0987B48F} — **0xE9889775 absent**. The NEXT census, 17:47:04.769 (line 506), 51 ms LATER, is the first one containing `VWKID 33 id=0xE9889775`.\n- 1.5x sample #2: DRAWPROBE 17:48:13.559 (line 1282). Preceding census 17:48:13.247 (lines 1275-1279) — **0xE9889775 absent**. Next census 17:48:13.605 (line 1291), 46 ms later — present.\nBoth 1.5x samples are PRE-SHOW paints. That is legitimate: 0x8A517556 carries `winflag_pbuff=yes`, so it paints into its own back buffer, and the project already recorded that a show detour fires BEFORE the visible bit.\n\n**3. By the project's own mode criterion, both 1.5x samples were taken with mayor mode OFF.** `src\\UiSpike.cpp:13015-13016`: `mayorHud = GetChildWindowFromIDRecursive(0xE9889775); mayorModeActive = (mayorHud && mayorHud->IsVisible());` with the in-file note that 0xE9889775 is \"shown in mayor mode and hidden in god mode ... vis=1 only in mayor mode. Verified across all THREE states.\" At 17:47:04.718 and 17:48:13.559 that predicate evaluated FALSE. The claim's conclusion inverts the codebase's own definition of the mode it asserts.\n\n**4. The tier comparison is not like-for-like — and the asymmetry is the whole point.** At 2x, sample #2 (`SC4UIScale-2026-08-16-172018.log:631`, 17:19:54.527, 204x22) sits 2.3 s AFTER the census at 17:19:52.207 that shows 0xE9889775 VISIBLE, with no intervening census removing it — a genuine HUD-visible, mayor-mode STEADY-STATE reading. The 1.5x pair has no such sample. So the good tier has a steady-state measurement and the bad tier has only transition-instant measurements. The claim pairs them as equivalent; they are not.\n\n**5. Null-vs-positive-control status.** The DRAWPROBE positive control is present (174827:471, 172018:476). The VWKID null is a TRUE null with a positive control: the same instrument DID print 0xE9889775 at 13 other timestamps in 174827, so it could have seen it. Separately, DRAWPROBE is BUDGETED (`gDrawProbe` from ini `[Probe] DrawProbe`, decremented per row, `src\\UiSpike.cpp:12101,3560-3567`) — 26 rows total at 1.5x, 12 at 2x, and the bar painted only twice in 23k paints. The absence of further rows is budget + paint rarity, not evidence of a stable rect.\n\n**Net effect on the investigation:** the widget IS a mayor-HUD widget (it lives under 0xE9889775, which the project verified is visible only in mayor mode) — that much survives. But the stated gap is NOT closed. The correct restatement is sharper: **the bar's rect has never been measured at 1.5x while 0xE9889775 was in the visible set.** Both 1.5x readings were captured with the HUD's visible bit still off, ~50 ms before it flipped — precisely the state in which a born-correct/pre-show path can leave a window correct that the game then re-lays on show. The next instrument should log the 0x8A517556 rect gated on `mayorHud->IsVisible()==true` at 1.5x (and re-log it several seconds into steady mayor mode), not on paint.\n\nNot one of the six dead attributions — this is about the provenance of the probe evidence, not a mechanism.",
   "wouldBreakIntegerTiers": false
  }
 },
 {
  "key": "v2:59c1b2fa15b92cdc18434395cb7503632b36a90e70cf4818ec0c2eb2ad69cc55",
  "result": {
   "refuted": true,
   "reason": "REFUTED — the quoted bytes are right, the headline is wrong, and the mechanism is tier-independent.\n\n1) RE-MEASURED, AND THE DISASM QUOTES ALL CHECK OUT. I disassembled 0x9BC325-0x9BC446 from the shipped exe (Apps\\SimCity 4.exe, ImageBase 0x400000). Verbatim confirmations: 0x9BC36F `push 0x10` / `call [eax+0x28]` / `jne 0x9BC398`, then 0x9BC37E `push 0x20` / `jne 0x9BC398`, else 0x9BC389 `mov ecx,[ebx+0xdc]` / `call [eax+0x30]`; 0x9BC398 `lea esi,[ebx+0xe8]`; 0x9BC39E four `movsd`. Plain path 0x9BC3B9-0x9BC3D6: `mov eax,[ebx+0x24]` / `mov ecx,[ebx+0x28]` / `[ebp-0x20]=eax` / `sub eax,[ebp-0x10]` / `add eax,[ebp-8]` / `[ebp-0x18]=eax` (same for t/b) — the window's r/b ([ebx+0x2c],[ebx+0x30]) are indeed never read inside the draw. 0x9BC3F6 `mov ecx,[ebx+0x6c]` … `call [eax+0x98]`. Ctor 0x9BC4BA zeroes [esi+0xE8..0xF4] and 0x9BC503 writes `[esi+0xF8]=0x12`. Flag getter is vtable 0x00ADF66C slot 10 (+0x28) = 0x9BC171 `mov eax,[ecx+0x20]; and eax,[esp+4]`, and holder is win+0xD8, so 0xD8+0x20 = win+0xF8. 9-slice `/3` at 0x9BC414/0x9BC423 into 0x8D8800. Nothing to refute on the numbers.\n\n2) THE CONSEQUENCE IS FALSE — MEASURED. \"The destination can never be clipped by the window\" collapses one call away. The Mayor-Rating controller's setter call at 0x007E8700 (`call [edx+0x10]`, immediately after 0x007E86E4 pushes id 0x8A517556 and IID 0xC12CEA13) lands on iface vtable 0x00ADF66C slot 4 = 0x009BC57E. That function stores the new image at [win+0xDC], then does `add esi,0xFFFFFF28` (esi -= 0xD8, i.e. back to the window), calls `win->vt[0xC0]` = 0x0099BCE1 = `lea eax,[ecx+0xA8]; ret` (a WINDOW rect), and passes it to 0x009BC447. And 0x009BC447 is the whole ballgame:\n    edx = arg->r - arg->l ; ecx = arg->b - arg->t\n    [win+0xE8]=0 ; [win+0xEC]=0 ; [win+0xF0]=edx ; [win+0xF4]=ecx\n    if ([win+0xF0] > image->vt[0x24]() /*GetWidth*/)  [win+0xF0] = GetWidth\n    if ([win+0xF4] > image->vt[0x28]() /*GetHeight*/) [win+0xF4] = GetHeight\nSo the STORED RECT is recomputed on EVERY SetBitmap as `(0, 0, min(winRect.w, imgW), min(winRect.h, imgH))`. The window's SIZE is the primary term; the image only clamps it DOWN. The clip the claim declares impossible is real — it just happens at SetImage time instead of draw time. Two further corollaries kill the claim's supporting premises: the .UI `imagerect=` (pre-scaled to 153 at 1.5x) is OVERWRITTEN for this widget the moment the controller calls SetBitmap, so it is not \"the source of truth\"; and the ctor's default 0x10 is not evidence of anything, it is simply why 0x9BC447 has to populate [0xE8] at all.\n\n3) OUR OWN SHIPPED CODE READS THE WINDOW SIZE AT DRAW TIME TOO. `src\\UiSpike.cpp` BmpCtxBltThunk (~lines 8737-8765) intercepts ctx slot 38 and rewrites dst, with `if (w*m > gBmpWinW) m = gBmpWinW / w;` — a mod-side path where the LIVE WINDOW bounds the destination. Any absolute statement about \"the blit\" that ignores the hook this project deploys on that exact slot is describing a binary the user is not running.\n\n4) KILLER TIER TEST — FAILS. The claim self-certifies \"tier-independent — no factor, no rounding, no division anywhere\". Its own conclusion (stored rect = 102) run at the confirmed-good tiers gives min(102,204)=102 → 50% fill at 2x and min(102,306)=102 → 33% at 3x, i.e. WORSE than the 102/153 = 66.7% it offers for 1.5x. 2x and 3x are user-confirmed correct. A mechanism with no factor in it cannot go silent at an integer factor, so it cannot be this defect. The only variable that can differ between tiers here is the WINDOW rect at [win+0xA8] — the one thing the claim rules out of consideration.\n\n5) IT IS DEAD ATTRIBUTION #5 IN NEW WORDS. \"The STORED RECT was 102\" is \"the window or crop is wrong\" restated, and it contradicts the live probe (DRAWPROBE win=0x8A517556 rect 153x17 at 1.5x, positive control present): a 153-wide window yields min(153,153)=153 and a full bar. The claim asserts the 102 without ever measuring [win+0xA8] or [win+0xE8] in mayor mode.\n\nSALVAGE (not a confirmation, a correction): the useful output of this run is 0x009BC447, which was not in any doc. It shows GZWinBMP is only \"dst-follows-src\" for script-bound art; for RUNTIME-supplied bitmaps the source rect is itself min(window, image), so `tools\\uimap\\BLIT-BEHAVIOUR.md` line 48 is incomplete and the parent's original lead (the window is not swept in mayor mode) is the surviving hypothesis, not the refuted one. The instrument that settles it is to log [win+0xA8] and [win+0xE8] for id 0x8A517556 at the moment 0x007E8700 returns, in MAYOR mode, at 1.5x and at 2x — the probe already run never established mode.",
   "wouldBreakIntegerTiers": true
  }
 },
 {
  "key": "v2:9cd448aa373acc429033a0cc7beff9f577a90a3b5b216afbcbbdf52d45c5ee2b",
  "result": {
   "refuted": true,
   "wouldBreakIntegerTiers": true,
   "reason": "REFUTED — measured counterexamples at BOTH integer tiers and in reverse at 1.5x.\n\nCREDIT FIRST (do not re-derive): every line citation checks out, and the min() formula is NOT invented. tools/research/_incoming/sdkgaps-06.md G6 (0x9BC57E): SetImage sets imagerect=(0,0,min(winW,imgW),min(winH,imgH)); G7: SetArea/SetSize never touch imagerect; G11: the draw is min(winW,imgW) x min(winH,imgH) from the art's top-left. The \"SetImage latches the crop\" law is real and worth keeping. It is not the tier split.\n\n1. KILLER TEST — A 2x SESSION IN EXACTLY THE CLAIM'S BROKEN STATE. _tests/captures/SC4UIScale-2026-08-15-105745.log, ScaleFactor=2.00: RATEANCHOR detour fires EXACTLY ONCE, l.219 [10:57:34.273] arrow L/T=(98,58) 21x9 stale=0 (1x geometry), and the HUD sweep lands 0.775 s LATER at l.423 [10:57:35.048] \"city panel 0xE9889775 - 30 windows scaled\". No second call, ever. Bit-for-bit the 1.5x shape the claim calls \"the lost race\". Under the claim this session drew 102/204 = EXACTLY 50% — the user's literal \"only half\". 2x is user-confirmed correct.\n\n2. 3x LOSES THE SAME RACE IN ~40 CONSECUTIVE SESSIONS. Every ScaleFactor=3.00 log on 2026-08-14/15 shows detour-logs=1, stale=0, pre-sweep. E.g. 2026-08-15-102324.log l.222 [10:22:57.356] (98,58) 21x9 stale=0 vs sweep l.427 [10:22:58.075]; same in -094614 (l.209 vs l.413) and 2026-08-14-161910 (l.220 vs l.424). The claim predicts a 33%-wide bar in essentially every 3x session. 3x is user-confirmed (task #143). The arithmetic does not go silent at either integer factor — the claim concedes this itself (\"3x would be 33% under the same race\"), which is a self-declared killer-test failure.\n\n3. REVERSE COUNTEREXAMPLE — 1.5x SESSIONS THAT WON THE RACE AND ARE STILL BROKEN. 2026-08-16-084246.log (1.5x): sweep l.427 [08:35:30.461], then post-sweep RATEANCHOR l.560 [08:35:59.948] stale=1 arrow (147,87), plus l.647 [08:37:01.960] and l.652 [08:38:01.956]. 2026-08-16-131006.log (1.5x): sweep l.427 [13:09:07.515], post-sweep stale=1 call l.601 [13:09:35.631]. Those post-sweep controller calls re-run SetImage with the window already 153x17, which by the claim's own G6 re-latches to min(153,153) x min(17,39) = FULL. Those sessions should have healed ~30 s in. The 1.5x defect was live all day and #176 is open, with no report of a self-healing or intermittent bar. Across the corpus the post-sweep-call rate is uncorrelated with tier (1.5x has MORE re-latches than 3x), so it cannot be the tier split.\n\n4. \"THE CONTROLLER RAN ONCE\" IS A GATED NULL WITH NO POSITIVE CONTROL. src/CodePatches.cpp:3969 gates the line on `if (stale || gAnchorLogs < 4)`, and line 3942 `if (!arrow) { break; }` returns BEFORE the log whenever GetChildWindowFromIDRecursive(0xCA5A415E) misses — while line 3990 still tail-calls gRatingUpdateOrig, so the real controller (and its SetBitmap) runs invisibly. No control was offered that the arrow lookup succeeds on every entry. The brief itself flags that the 1.5x capture's mode is unestablished — mayor vs god mode is precisely the condition under which that child lookup could miss, so the claim's headline null is weakest exactly where the defect lives.\n\n5. THE CROP WAS NEVER MEASURED. DRAWPROBE prints the WINDOW rect only (153x17 / 204x22, class=00ADF6A0). imagerect (+0xE8) appears in no capture. \"The crop latched to 102x11\" is pure inference — and \"the window or crop is wrong\" is already dead attribution #5 (probe says proportional). Re-badging it as \"the crop is stale\" is the same attribution in new words.\n\n6. TWO MIS-READS. (a) l.630 `arrow L/T=(196,116) 2x18` — the printed WIDTH is 2, not 42; the claim reads this as healthy 2x geometry, but 21->2 is a collapse, and the identical artifact appears at 1.5x (`2x14` then `1x14` in 084246), so it is not a tier discriminator. (b) 2x DRAWPROBE already reads 204x22 at l.551 [17:19:52.148], 2.3 s BEFORE the alleged healing call at 17:19:54.496 — the ordering narrative is not what the log shows. Minor: \"75 s\" is 72.6 s; the 1.5x sweep is at 17:47:01.741 (l.434-435), not .737.\n\n7. SYMPTOM POLARITY IS BACKWARDS. The claim's own arithmetic assigns exactly-half (102/204) to 2x and two-thirds (102/153) to 1.5x. The user reports \"only half\" at 1.5x and CORRECT at 2x.\n\nThe tier split is not a controller/sweep race. Next place to look: the imagerect actually stored at +0xE8 must be INSTRUMENTED (it never has been), and the mayor-vs-god mode of the 1.5x capture must be established before any further reasoning from these two logs."
  }
 },
 {
  "key": "v2:e813135217b89f414323d4e5ffd07537320e58ceac5536c27e32bce68f6c6fd3",
  "result": {
   "refuted": true,
   "reason": "REFUTED on the killer test, plus one measured factual error in the load-bearing evidence.\n\nWHAT VERIFIED (re-measured against SimCity 4.exe, base 0x400000, via tools/research/disasm_at.py):\n- 0x9BC447 is exactly as quoted: edx=[arg+8]-[arg+0], ecx=[arg+0xC]-[arg+4]; eax=esi+0xE8; `and [eax],0` / `and [eax+4],0`; [eax+8]=edx; [eax+0xC]=ecx; then clamp [esi+0xF0] against img vt[0x24] (GetWidth) and [esi+0xF4] against vt[0x28] (GetHeight).\n- Direct-caller scan reproduces EXACTLY 2 callers of 0x9BC447 (0x9BC56E, 0x9BC5B8) and 0 dword refs. Positive control 0x8D8800 -> 6 callers including 0x9BC439.\n- Iface vtable 0x00ADF66C slot 4 (+0x10) = 0x9BC57E; SetBitmap does `add esi,0xFFFFFF28` (-0xD8), `call [vt+0xC0]` = 0x99BCE1 = `lea eax,[ecx+0xA8]; ret` (GetArea), then 0x9BC447, then [vt+0x170]. Controller reaches it at 0x7E8700 `call [edx+0x10]` on the iface from GetChildAs(0x8a517556, 0xc12cea13) at 0x7E86E4. Buffer Init confirmed at 0x7E857A `call [ebp+0xc]` (cIGZBuffer::Init slot 3) with GetWidth/GetHeight pushed.\n- Window vtable 0x00ADF6A0 slot 55 (+0xDC) = 0x9BC0B8 = bare `jmp 0x99C837`. 0x99C837 writes only +0xA8..+0xB4, calls [vt+0x190] with push 1 when ([esi+0xCA]&1) and the size changed, then [vt+0x168], and ALSO an unconditional [vt+0x188] = 0x99D5B7 that the claim omitted. GZWinBMP overrides none of slots 90/97/98/100, so SetArea genuinely never refreshes +0xE8. That property is REAL and worth documenting as an ordering hazard.\n\nWHY IT IS STILL REFUTED:\n\n1. KILLER TEST FAILED — the mechanism has no tier term. Latch = min(areaW, imgW). imgW = art width = round(102*f) at every tier (153/204/306); areaW = 102 if unswept, round(102*f) if swept. f cancels inside the min(). The gate is binary and f-independent (\"was the window swept before SetBitmap?\"), so when it fires it fires at EVERY tier: 1/3 of the bar missing at 1.5x, HALF missing at 2x, TWO-THIRDS missing at 3x. The claim's own TIERS line concedes exactly this. 2x is user-confirmed correct and 3x was eyes-on confirmed at 3840x2160. No mechanism that amputates half the 2x bar can be this defect. The claim offers no tier-dependent term and none exists.\n\n2. \"The only writer is 0x9BC447\" is FALSE, and the scan that produced it could not see the other writer by construction. 0x9BC103 = cIGZWinBMP::SetImageRect, iface vtable slot 8 (+0x20) at 0x00ADF68C, also present in vtables 0x00AB9970 and 0x00ABA760. With ecx = window+0xD8 it does `lea eax,[ebx+0x10]` (= window+0xE8), four movsd from the caller-supplied rect, then clamps [+0xE8]>=0, [+0xEC]>=0, [+0xF0]<=img GetWidth, [+0xF4]<=img GetHeight. 0x9BC15F at slot 9 is the matching GetImageRect. Measured: 0x9BC103 has 0 direct E8/E9 callers and 3 dword (vtable) references — it is virtual-only dispatch, invisible to a direct-caller scan. The quoted positive control (0x8D8800 -> 6 callers) validated the direct-call scanner only; it never validated the much stronger claim \"only writer in the whole image\". That is a null with the wrong positive control. Corollary: 0x9BC447 force-zeroes L/T, so it cannot implement the nonzero-l,t imagerects the project has already documented (UI-ART-BINDING.md:133) — 0x9BC103 does. Our own DLL is a further writer of +0xE8..+0xF4 (src/UiSpike.cpp BMPRECT, ~:9042-9080). So \"the pre-scaled imagerect is DEAD DATA\" is false as a general statement; at most it is overwritten for THIS widget once the controller's first SetBitmap runs.\n\n3. \"Resizing enlarges the FRAME and not the DRAWING\" contradicts the measured draw. Plot 0x9BC325, 0x9BC3B9-0x9BC3D4: dst = {areaL, areaT, areaL+srcW, areaT+srcH}. The window rect supplies only the ORIGIN; nothing draws a 153-wide frame. A stale 102-wide src paints nothing at all in the right 51px, and 0x8A517556 IS the groove/track (DYNAMIC-CONTROLS.md:36), so the truncation would take the track with it. Predicted symptom = a physically shorter, amputated bar with a gap; reported symptom = a full-length bar that only fills halfway. Mismatch.\n\n4. \"Permanently, until the next SetBitmap\" is unproven and probably false. The controller rebuilds and Inits a buffer on every rating refresh and calls SetBitmap; 0x9BC447 then recomputes the latch from the CURRENT area — unless the buffer pointer is byte-identical, hitting the early-out at 0x9BC586 (`cmp edi,[esi+4] / je 0x9BC5C7`). The claim never establishes buffer reuse (it does not quote the create/release path at all), so the load-bearing \"permanent\" step is unsupported. Any stale latch would heal on the next rating tick.\n\nFAIRNESS: this is NOT a rebadge of dead attribution #5. DRAWPROBE prints w->GetL/GetT/GetW/GetH (src/UiSpike.cpp:3563) — the WINDOW rect, not +0xE8 — so #5's refutation never covered the source rect. This was a legitimately new sub-hypothesis; it dies on the tier test, not on redundancy.\n\nWORTH KEEPING (not the defect): SetArea does not refresh GZWinBMP's +0xE8 src rect. That is a genuine, previously-undocumented ordering hazard for any future GZWinBMP resize-after-bitmap work, and the SetImageRect entry point (iface slot 8, 0x9BC103) is the correct lever if such a case is ever found.",
   "wouldBreakIntegerTiers": true
  }
 }
]