# WINNING CORPUS — who actually supplies each `.UI` script

*Generated by `tools\uiscripts\winning_corpus.py`. REPORT ONLY: nothing here has been acted on.*

The builders read `extracted\` (game archives only). This resolves the real load-order winner per TGI.

| bucket | count |
|---|---|
| `.UI` TGIs seen | 333 |
| won by the game archives | 78 |
| won by OUR packages | 255 |
| **won by a THIRD PARTY** | **0** |

## Third-party winners

| TGI | winner |
|---|---|

## TARGETS we build from the WRONG SOURCE

*(none, or shapes unreadable)*

## What to do

1. Build the affected dialogs from the **winner**, into
   `zzz-SC4UIScale\`, gated on their mod (`ScaleTier::kThirdPartyDeps`) exactly like `SaveWarningUI`.
2. Add a build-time assert: **a TARGET's winning script must be the one the builder read.** That single check would have caught
   task #79c and both CAM dialogs.
3. Re-derive art referrers from winners, not from stock — exclusivity verdicts computed from a stock-only corpus are computed from fiction.

